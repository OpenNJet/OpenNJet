

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_dyn_range_api_parser.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */


static bool parse_dyn_range_api_action(njt_pool_t *pool, parse_state_t *parse_state, dyn_range_api_action_t *out, js2c_parse_error_t *err_ret) {
    js2c_check_type(JSMN_STRING);
    if (current_string_is(parse_state, "add")) {
        *out = DYN_RANGE_API_ACTION_ADD;
    } else if (current_string_is(parse_state, "del")) {
        *out = DYN_RANGE_API_ACTION_DEL;
    } else {
        LOG_ERROR_JSON_PARSE(UNKNOWN_ENUM_VALUE_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown enum value in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
        return true;
    }
    parse_state->current_token += 1;
    return false;
}


static bool parse_dyn_range_api_type(njt_pool_t *pool, parse_state_t *parse_state, dyn_range_api_type_t *out, js2c_parse_error_t *err_ret) {
    js2c_check_type(JSMN_STRING);
    if (current_string_is(parse_state, "tcp")) {
        *out = DYN_RANGE_API_TYPE_TCP;
    } else if (current_string_is(parse_state, "udp")) {
        *out = DYN_RANGE_API_TYPE_UDP;
    } else {
        LOG_ERROR_JSON_PARSE(UNKNOWN_ENUM_VALUE_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown enum value in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
        return true;
    }
    parse_state->current_token += 1;
    return false;
}


static bool parse_dyn_range_api_family(njt_pool_t *pool, parse_state_t *parse_state, dyn_range_api_family_t *out, js2c_parse_error_t *err_ret) {
    js2c_check_type(JSMN_STRING);
    if (current_string_is(parse_state, "ipv4")) {
        *out = DYN_RANGE_API_FAMILY_IPV_4;
    } else if (current_string_is(parse_state, "ipv6")) {
        *out = DYN_RANGE_API_FAMILY_IPV_6;
    } else {
        LOG_ERROR_JSON_PARSE(UNKNOWN_ENUM_VALUE_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown enum value in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
        return true;
    }
    parse_state->current_token += 1;
    return false;
}


static bool parse_dyn_range_api(njt_pool_t *pool, parse_state_t *parse_state, dyn_range_api_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "action")) {
            js2c_check_field_set(out->is_action_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "action";
            if (parse_dyn_range_api_action(pool, parse_state, (&out->action), err_ret)) {
                return true;
            }
            out->is_action_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "type")) {
            js2c_check_field_set(out->is_type_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "type";
            if (parse_dyn_range_api_type(pool, parse_state, (&out->type), err_ret)) {
                return true;
            }
            out->is_type_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "family")) {
            js2c_check_field_set(out->is_family_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "family";
            js2c_null_check();
            if (parse_dyn_range_api_family(pool, parse_state, (&out->family), err_ret)) {
                return true;
            }
            out->is_family_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "src_ports")) {
            js2c_check_field_set(out->is_src_ports_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "src_ports";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->src_ports))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->src_ports))->data);
            ((&out->src_ports))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->src_ports), 0, ((&out->src_ports))->len, err_ret)) {
                return true;
            }
            out->is_src_ports_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "dst_port")) {
            js2c_check_field_set(out->is_dst_port_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "dst_port";
            int64_t int_parse_tmp;
            if (builtin_parse_signed(pool, parse_state, true, false, 10, &int_parse_tmp, err_ret)) {
                return true;
            }
            js2c_int_range_check_min(1LL);
            *(&out->dst_port) = int_parse_tmp;
            out->is_dst_port_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_src_ports_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': src_ports", parse_state->current_key);
        return true;
    }
    // set default
    if (!out->is_family_set) {
        out->family = DYN_RANGE_API_FAMILY_IPV_4;
    }
    parse_state->current_token = saved_current_token;
    return false;
}

// BEGIN GET_JSON_LENGTH ENUM

static void get_json_length_dyn_range_api_action(njt_pool_t *pool, dyn_range_api_action_t *out, size_t *length, njt_int_t flags) {
    if (*out == DYN_RANGE_API_ACTION_ADD) {
        // "add"
        *length += 3 + 2;
        return;
    }
    if (*out == DYN_RANGE_API_ACTION_DEL) {
        // "del"
        *length += 3 + 2;
        return;
    }
}
// BEGIN GET_JSON_LENGTH ENUM

static void get_json_length_dyn_range_api_type(njt_pool_t *pool, dyn_range_api_type_t *out, size_t *length, njt_int_t flags) {
    if (*out == DYN_RANGE_API_TYPE_TCP) {
        // "tcp"
        *length += 3 + 2;
        return;
    }
    if (*out == DYN_RANGE_API_TYPE_UDP) {
        // "udp"
        *length += 3 + 2;
        return;
    }
}
// BEGIN GET_JSON_LENGTH ENUM

static void get_json_length_dyn_range_api_family(njt_pool_t *pool, dyn_range_api_family_t *out, size_t *length, njt_int_t flags) {
    if (*out == DYN_RANGE_API_FAMILY_IPV_4) {
        // "ipv4"
        *length += 4 + 2;
        return;
    }
    if (*out == DYN_RANGE_API_FAMILY_IPV_6) {
        // "ipv6"
        *length += 4 + 2;
        return;
    }
}

static void get_json_length_dyn_range_api_src_ports(njt_pool_t *pool, dyn_range_api_src_ports_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dyn_range_api_dst_port(njt_pool_t *pool, dyn_range_api_dst_port_t *out, size_t *length, njt_int_t flags) {
    u_char str[24];
    u_char *cur;
    cur = njt_sprintf(str, "%L", *out);
    *length += cur - str;
}

static void get_json_length_dyn_range_api(njt_pool_t *pool, dyn_range_api_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_action_set ? 0 : 1;
    if (omit == 0) {
        *length += (6 + 3); // "action": 
        get_json_length_dyn_range_api_action(pool, (&out->action), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_type_set ? 0 : 1;
    if (omit == 0) {
        *length += (4 + 3); // "type": 
        get_json_length_dyn_range_api_type(pool, (&out->type), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_family_set ? 0 : 1;
    if (omit == 0) {
        *length += (6 + 3); // "family": 
        get_json_length_dyn_range_api_family(pool, (&out->family), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_src_ports_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->src_ports.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (9 + 3); // "src_ports": 
        get_json_length_dyn_range_api_src_ports(pool, (&out->src_ports), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_dst_port_set ? 0 : 1;
    if (omit == 0) {
        *length += (8 + 3); // "dst_port": 
        get_json_length_dyn_range_api_dst_port(pool, (&out->dst_port), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

dyn_range_api_action_t get_dyn_range_api_action(dyn_range_api_t *out) {
    return out->action;
}

dyn_range_api_type_t get_dyn_range_api_type(dyn_range_api_t *out) {
    return out->type;
}

dyn_range_api_family_t get_dyn_range_api_family(dyn_range_api_t *out) {
    return out->family;
}

dyn_range_api_src_ports_t* get_dyn_range_api_src_ports(dyn_range_api_t *out) {
    return &out->src_ports;
}

dyn_range_api_dst_port_t get_dyn_range_api_dst_port(dyn_range_api_t *out) {
    return out->dst_port;
}
void set_dyn_range_api_action(dyn_range_api_t* obj, dyn_range_api_action_t field) {
    obj->action = field;
    obj->is_action_set = 1;
}
void set_dyn_range_api_type(dyn_range_api_t* obj, dyn_range_api_type_t field) {
    obj->type = field;
    obj->is_type_set = 1;
}
void set_dyn_range_api_family(dyn_range_api_t* obj, dyn_range_api_family_t field) {
    obj->family = field;
    obj->is_family_set = 1;
}
void set_dyn_range_api_src_ports(dyn_range_api_t* obj, dyn_range_api_src_ports_t* field) {
    njt_memcpy(&obj->src_ports, field, sizeof(njt_str_t));
    obj->is_src_ports_set = 1;
}
void set_dyn_range_api_dst_port(dyn_range_api_t* obj, dyn_range_api_dst_port_t field) {
    obj->dst_port = field;
    obj->is_dst_port_set = 1;
}
dyn_range_api_t* create_dyn_range_api(njt_pool_t *pool) {
    dyn_range_api_t* out = njt_pcalloc(pool, sizeof(dyn_range_api_t));
    return out;
}

static void to_oneline_json_dyn_range_api_action(njt_pool_t *pool, dyn_range_api_action_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out == DYN_RANGE_API_ACTION_ADD) {
        cur = njt_sprintf(cur, "\"add\"");
        buf->len += 3 + 2;
        return;
    }
    if (*out == DYN_RANGE_API_ACTION_DEL) {
        cur = njt_sprintf(cur, "\"del\"");
        buf->len += 3 + 2;
        return;
    }
}

static void to_oneline_json_dyn_range_api_type(njt_pool_t *pool, dyn_range_api_type_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out == DYN_RANGE_API_TYPE_TCP) {
        cur = njt_sprintf(cur, "\"tcp\"");
        buf->len += 3 + 2;
        return;
    }
    if (*out == DYN_RANGE_API_TYPE_UDP) {
        cur = njt_sprintf(cur, "\"udp\"");
        buf->len += 3 + 2;
        return;
    }
}

static void to_oneline_json_dyn_range_api_family(njt_pool_t *pool, dyn_range_api_family_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out == DYN_RANGE_API_FAMILY_IPV_4) {
        cur = njt_sprintf(cur, "\"ipv4\"");
        buf->len += 4 + 2;
        return;
    }
    if (*out == DYN_RANGE_API_FAMILY_IPV_6) {
        cur = njt_sprintf(cur, "\"ipv6\"");
        buf->len += 4 + 2;
        return;
    }
}

static void to_oneline_json_dyn_range_api_src_ports(njt_pool_t *pool, dyn_range_api_src_ports_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dyn_range_api_dst_port(njt_pool_t *pool, dyn_range_api_dst_port_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    cur = njt_sprintf(cur, "%L", *out);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dyn_range_api(njt_pool_t *pool, dyn_range_api_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_action_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"action\":");
        buf->len = cur - buf->data;
        to_oneline_json_dyn_range_api_action(pool, (&out->action), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_type_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"type\":");
        buf->len = cur - buf->data;
        to_oneline_json_dyn_range_api_type(pool, (&out->type), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_family_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"family\":");
        buf->len = cur - buf->data;
        to_oneline_json_dyn_range_api_family(pool, (&out->family), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_src_ports_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->src_ports.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"src_ports\":");
        buf->len = cur - buf->data;
        to_oneline_json_dyn_range_api_src_ports(pool, (&out->src_ports), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_dst_port_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"dst_port\":");
        buf->len = cur - buf->data;
        to_oneline_json_dyn_range_api_dst_port(pool, (&out->dst_port), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
dyn_range_api_t* json_parse_dyn_range_api(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    dyn_range_api_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(dyn_range_api_t));;
    if (parse_dyn_range_api(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_dyn_range_api(njt_pool_t *pool, dyn_range_api_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_dyn_range_api(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_dyn_range_api(pool, out, json_str, flags);
    return json_str;
}
