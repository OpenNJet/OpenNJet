

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_parser_cache.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */


static bool parse_cache_caches_item(njt_pool_t *pool, parse_state_t *parse_state, cache_caches_item_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "location_name")) {
            js2c_check_field_set(out->is_location_name_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "location_name";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->location_name))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->location_name))->data);
            ((&out->location_name))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->location_name), 0, ((&out->location_name))->len, err_ret)) {
                return true;
            }
            out->is_location_name_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "backend_server")) {
            js2c_check_field_set(out->is_backend_server_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "backend_server";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->backend_server))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->backend_server))->data);
            ((&out->backend_server))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->backend_server), 0, ((&out->backend_server))->len, err_ret)) {
                return true;
            }
            out->is_backend_server_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "status")) {
            js2c_check_field_set(out->is_status_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "status";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->status))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->status))->data);
            ((&out->status))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->status), 0, ((&out->status))->len, err_ret)) {
                return true;
            }
            out->is_status_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "download_ratio")) {
            js2c_check_field_set(out->is_download_ratio_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "download_ratio";
            int64_t int_parse_tmp;
            if (builtin_parse_signed(pool, parse_state, true, false, 10, &int_parse_tmp, err_ret)) {
                return true;
            }
            js2c_int_range_check_min(0LL);
            js2c_int_range_check_max(100LL);
            *(&out->download_ratio) = int_parse_tmp;
            out->is_download_ratio_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_location_name_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': location_name", parse_state->current_key);
        return true;
    }
    if (!out->is_backend_server_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': backend_server", parse_state->current_key);
        return true;
    }
    if (!out->is_status_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': status", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static bool parse_cache_caches(njt_pool_t *pool, parse_state_t *parse_state, cache_caches_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        ((cache_caches_item_t**)out->elts)[i] = njt_pcalloc(pool, sizeof(cache_caches_item_t));
        memset(((cache_caches_item_t**)out->elts)[i], 0, sizeof(cache_caches_item_t));
        if (parse_cache_caches_item(pool, parse_state, ((cache_caches_item_t**)out->elts)[i], err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_cache(njt_pool_t *pool, parse_state_t *parse_state, cache_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "caches")) {
            js2c_check_field_set(out->is_caches_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "caches";
            out->caches = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(cache_caches_item_t*));
            js2c_malloc_check(out->caches);

            if (parse_cache_caches(pool, parse_state, (out->caches), err_ret)) {
                return true;
            }
            out->is_caches_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_caches_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': caches", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_cache_caches_item_location_name(njt_pool_t *pool, cache_caches_item_location_name_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_cache_caches_item_backend_server(njt_pool_t *pool, cache_caches_item_backend_server_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_cache_caches_item_status(njt_pool_t *pool, cache_caches_item_status_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_cache_caches_item_download_ratio(njt_pool_t *pool, cache_caches_item_download_ratio_t *out, size_t *length, njt_int_t flags) {
    u_char str[24];
    u_char *cur;
    cur = njt_sprintf(str, "%L", *out);
    *length += cur - str;
}

static void get_json_length_cache_caches_item(njt_pool_t *pool, cache_caches_item_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_location_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->location_name.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (13 + 3); // "location_name": 
        get_json_length_cache_caches_item_location_name(pool, (&out->location_name), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_backend_server_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->backend_server.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (14 + 3); // "backend_server": 
        get_json_length_cache_caches_item_backend_server(pool, (&out->backend_server), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_status_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->status.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (6 + 3); // "status": 
        get_json_length_cache_caches_item_status(pool, (&out->status), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_download_ratio_set ? 0 : 1;
    if (omit == 0) {
        *length += (14 + 3); // "download_ratio": 
        get_json_length_cache_caches_item_download_ratio(pool, (&out->download_ratio), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

static void get_json_length_cache_caches(njt_pool_t *pool, cache_caches_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((cache_caches_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_cache_caches_item(pool, ((cache_caches_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_cache(njt_pool_t *pool, cache_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_caches_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->caches) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (6 + 3); // "caches": 
        get_json_length_cache_caches(pool, (out->caches), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

cache_caches_item_location_name_t* get_cache_caches_item_location_name(cache_caches_item_t *out) {
    return &out->location_name;
}

cache_caches_item_backend_server_t* get_cache_caches_item_backend_server(cache_caches_item_t *out) {
    return &out->backend_server;
}

cache_caches_item_status_t* get_cache_caches_item_status(cache_caches_item_t *out) {
    return &out->status;
}

cache_caches_item_download_ratio_t get_cache_caches_item_download_ratio(cache_caches_item_t *out) {
    return out->download_ratio;
}
cache_caches_item_t* get_cache_caches_item(cache_caches_t *out, size_t idx) {
    return ((cache_caches_item_t**)out->elts)[idx];

}

cache_caches_t* get_cache_caches(cache_t *out) {
    return out->caches;
}
void set_cache_caches_item_location_name(cache_caches_item_t* obj, cache_caches_item_location_name_t* field) {
    njt_memcpy(&obj->location_name, field, sizeof(njt_str_t));
    obj->is_location_name_set = 1;
}
void set_cache_caches_item_backend_server(cache_caches_item_t* obj, cache_caches_item_backend_server_t* field) {
    njt_memcpy(&obj->backend_server, field, sizeof(njt_str_t));
    obj->is_backend_server_set = 1;
}
void set_cache_caches_item_status(cache_caches_item_t* obj, cache_caches_item_status_t* field) {
    njt_memcpy(&obj->status, field, sizeof(njt_str_t));
    obj->is_status_set = 1;
}
void set_cache_caches_item_download_ratio(cache_caches_item_t* obj, cache_caches_item_download_ratio_t field) {
    obj->download_ratio = field;
    obj->is_download_ratio_set = 1;
}
cache_caches_item_t* create_cache_caches_item(njt_pool_t *pool) {
    cache_caches_item_t* out = njt_pcalloc(pool, sizeof(cache_caches_item_t));
    return out;
}
int add_item_cache_caches(cache_caches_t *src, cache_caches_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

cache_caches_t* create_cache_caches(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(cache_caches_item_t*));
}
void set_cache_caches(cache_t* obj, cache_caches_t* field) {
    obj->caches = field;
    obj->is_caches_set = 1;
}
cache_t* create_cache(njt_pool_t *pool) {
    cache_t* out = njt_pcalloc(pool, sizeof(cache_t));
    return out;
}

static void to_oneline_json_cache_caches_item_location_name(njt_pool_t *pool, cache_caches_item_location_name_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_cache_caches_item_backend_server(njt_pool_t *pool, cache_caches_item_backend_server_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_cache_caches_item_status(njt_pool_t *pool, cache_caches_item_status_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_cache_caches_item_download_ratio(njt_pool_t *pool, cache_caches_item_download_ratio_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    cur = njt_sprintf(cur, "%L", *out);
    buf->len = cur - buf->data;
}

static void to_oneline_json_cache_caches_item(njt_pool_t *pool, cache_caches_item_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_location_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->location_name.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"location_name\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_caches_item_location_name(pool, (&out->location_name), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_backend_server_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->backend_server.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"backend_server\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_caches_item_backend_server(pool, (&out->backend_server), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_status_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->status.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"status\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_caches_item_status(pool, (&out->status), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_download_ratio_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"download_ratio\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_caches_item_download_ratio(pool, (&out->download_ratio), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}

static void to_oneline_json_cache_caches(njt_pool_t *pool, cache_caches_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((cache_caches_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_cache_caches_item(pool, ((cache_caches_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_cache(njt_pool_t *pool, cache_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_caches_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->caches) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"caches\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_caches(pool, (out->caches), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
cache_t* json_parse_cache(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    cache_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(cache_t));;
    if (parse_cache(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_cache(njt_pool_t *pool, cache_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_cache(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_cache(pool, out, json_str, flags);
    return json_str;
}
