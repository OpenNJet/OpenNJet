

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_parser_cache_api.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */


static bool parse_cache_api_type(njt_pool_t *pool, parse_state_t *parse_state, cache_api_type_t *out, js2c_parse_error_t *err_ret) {
    js2c_check_type(JSMN_STRING);
    if (current_string_is(parse_state, "add")) {
        *out = CACHE_API_TYPE_ADD;
    } else if (current_string_is(parse_state, "del")) {
        *out = CACHE_API_TYPE_DEL;
    } else if (current_string_is(parse_state, "download_status")) {
        *out = CACHE_API_TYPE_DOWNLOAD_STATUS;
    } else {
        LOG_ERROR_JSON_PARSE(UNKNOWN_ENUM_VALUE_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown enum value in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
        return true;
    }
    parse_state->current_token += 1;
    return false;
}


static bool parse_cache_api(njt_pool_t *pool, parse_state_t *parse_state, cache_api_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "type")) {
            js2c_check_field_set(out->is_type_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "type";
            if (parse_cache_api_type(pool, parse_state, (&out->type), err_ret)) {
                return true;
            }
            out->is_type_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "location_name")) {
            js2c_check_field_set(out->is_location_name_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "location_name";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->location_name))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->location_name))->data);
            ((&out->location_name))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->location_name), 0, ((&out->location_name))->len, err_ret)) {
                return true;
            }
            out->is_location_name_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "backend_server")) {
            js2c_check_field_set(out->is_backend_server_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "backend_server";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->backend_server))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->backend_server))->data);
            ((&out->backend_server))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->backend_server), 0, ((&out->backend_server))->len, err_ret)) {
                return true;
            }
            out->is_backend_server_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_type_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': type", parse_state->current_key);
        return true;
    }
    if (!out->is_location_name_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': location_name", parse_state->current_key);
        return true;
    }
    if (!out->is_backend_server_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': backend_server", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}

// BEGIN GET_JSON_LENGTH ENUM

static void get_json_length_cache_api_type(njt_pool_t *pool, cache_api_type_t *out, size_t *length, njt_int_t flags) {
    if (*out == CACHE_API_TYPE_ADD) {
        // "add"
        *length += 3 + 2;
        return;
    }
    if (*out == CACHE_API_TYPE_DEL) {
        // "del"
        *length += 3 + 2;
        return;
    }
    if (*out == CACHE_API_TYPE_DOWNLOAD_STATUS) {
        // "download_status"
        *length += 15 + 2;
        return;
    }
}

static void get_json_length_cache_api_location_name(njt_pool_t *pool, cache_api_location_name_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_cache_api_backend_server(njt_pool_t *pool, cache_api_backend_server_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_cache_api(njt_pool_t *pool, cache_api_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_type_set ? 0 : 1;
    if (omit == 0) {
        *length += (4 + 3); // "type": 
        get_json_length_cache_api_type(pool, (&out->type), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_location_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->location_name.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (13 + 3); // "location_name": 
        get_json_length_cache_api_location_name(pool, (&out->location_name), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_backend_server_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->backend_server.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (14 + 3); // "backend_server": 
        get_json_length_cache_api_backend_server(pool, (&out->backend_server), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

cache_api_type_t get_cache_api_type(cache_api_t *out) {
    return out->type;
}

cache_api_location_name_t* get_cache_api_location_name(cache_api_t *out) {
    return &out->location_name;
}

cache_api_backend_server_t* get_cache_api_backend_server(cache_api_t *out) {
    return &out->backend_server;
}
void set_cache_api_type(cache_api_t* obj, cache_api_type_t field) {
    obj->type = field;
    obj->is_type_set = 1;
}
void set_cache_api_location_name(cache_api_t* obj, cache_api_location_name_t* field) {
    njt_memcpy(&obj->location_name, field, sizeof(njt_str_t));
    obj->is_location_name_set = 1;
}
void set_cache_api_backend_server(cache_api_t* obj, cache_api_backend_server_t* field) {
    njt_memcpy(&obj->backend_server, field, sizeof(njt_str_t));
    obj->is_backend_server_set = 1;
}
cache_api_t* create_cache_api(njt_pool_t *pool) {
    cache_api_t* out = njt_pcalloc(pool, sizeof(cache_api_t));
    return out;
}

static void to_oneline_json_cache_api_type(njt_pool_t *pool, cache_api_type_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out == CACHE_API_TYPE_ADD) {
        cur = njt_sprintf(cur, "\"add\"");
        buf->len += 3 + 2;
        return;
    }
    if (*out == CACHE_API_TYPE_DEL) {
        cur = njt_sprintf(cur, "\"del\"");
        buf->len += 3 + 2;
        return;
    }
    if (*out == CACHE_API_TYPE_DOWNLOAD_STATUS) {
        cur = njt_sprintf(cur, "\"download_status\"");
        buf->len += 15 + 2;
        return;
    }
}

static void to_oneline_json_cache_api_location_name(njt_pool_t *pool, cache_api_location_name_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_cache_api_backend_server(njt_pool_t *pool, cache_api_backend_server_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_cache_api(njt_pool_t *pool, cache_api_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_type_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"type\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_api_type(pool, (&out->type), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_location_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->location_name.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"location_name\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_api_location_name(pool, (&out->location_name), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_backend_server_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->backend_server.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"backend_server\":");
        buf->len = cur - buf->data;
        to_oneline_json_cache_api_backend_server(pool, (&out->backend_server), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
cache_api_t* json_parse_cache_api(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    cache_api_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(cache_api_t));;
    if (parse_cache_api(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_cache_api(njt_pool_t *pool, cache_api_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_cache_api(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_cache_api(pool, out, json_str, flags);
    return json_str;
}
