

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_dyn_modsecurity_parser.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */

static bool parse_dynmodsecurity_locationDef(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_locationDef_t *out, js2c_parse_error_t *err_ret); //forward decl for public definition
static void get_json_length_dynmodsecurity_locationDef(njt_pool_t *pool, dynmodsecurity_locationDef_t *out, size_t *length, njt_int_t flags); //forward decl for public definition
static void to_oneline_json_dynmodsecurity_locationDef(njt_pool_t *pool, dynmodsecurity_locationDef_t *out, njt_str_t *buf, njt_int_t flags); //forward decl for public definition

static bool parse_dynmodsecurity_locationDef_modsecurity(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_locationDef_modsecurity_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "enable")) {
            js2c_check_field_set(out->is_enable_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "enable";
            if (builtin_parse_bool(pool, parse_state, (&out->enable), err_ret)) {
                return true;
            }
            out->is_enable_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "rules_file")) {
            js2c_check_field_set(out->is_rules_file_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "rules_file";
            js2c_null_check();
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->rules_file))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->rules_file))->data);
            ((&out->rules_file))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->rules_file), 0, ((&out->rules_file))->len, err_ret)) {
                return true;
            }
            out->is_rules_file_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "rules_remote")) {
            js2c_check_field_set(out->is_rules_remote_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "rules_remote";
            js2c_null_check();
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->rules_remote))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->rules_remote))->data);
            ((&out->rules_remote))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->rules_remote), 0, ((&out->rules_remote))->len, err_ret)) {
                return true;
            }
            out->is_rules_remote_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "rules")) {
            js2c_check_field_set(out->is_rules_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "rules";
            js2c_null_check();
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->rules))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->rules))->data);
            ((&out->rules))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->rules), 0, ((&out->rules))->len, err_ret)) {
                return true;
            }
            out->is_rules_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "transaction_id")) {
            js2c_check_field_set(out->is_transaction_id_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "transaction_id";
            js2c_null_check();
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->transaction_id))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->transaction_id))->data);
            ((&out->transaction_id))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->transaction_id), 0, ((&out->transaction_id))->len, err_ret)) {
                return true;
            }
            out->is_transaction_id_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_enable_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': enable", parse_state->current_key);
        return true;
    }
    // set default
    if (!out->is_rules_file_set) {
        size_t token_size = strlen("None");
        (out->rules_file).data = (u_char*)njt_pcalloc(pool, token_size + 1);
        js2c_malloc_check((out->rules_file).data);
        (out->rules_file).len = token_size;
        if (out->rules_file.len == 0) {
            (out->rules_file).data[0] = 0;
        }
        if (token_size > 0) {
            njt_memcpy(out->rules_file.data, "None", token_size);
        }
    }
    // set default
    if (!out->is_rules_remote_set) {
        size_t token_size = strlen("None");
        (out->rules_remote).data = (u_char*)njt_pcalloc(pool, token_size + 1);
        js2c_malloc_check((out->rules_remote).data);
        (out->rules_remote).len = token_size;
        if (out->rules_remote.len == 0) {
            (out->rules_remote).data[0] = 0;
        }
        if (token_size > 0) {
            njt_memcpy(out->rules_remote.data, "None", token_size);
        }
    }
    // set default
    if (!out->is_rules_set) {
        size_t token_size = strlen("None");
        (out->rules).data = (u_char*)njt_pcalloc(pool, token_size + 1);
        js2c_malloc_check((out->rules).data);
        (out->rules).len = token_size;
        if (out->rules.len == 0) {
            (out->rules).data[0] = 0;
        }
        if (token_size > 0) {
            njt_memcpy(out->rules.data, "None", token_size);
        }
    }
    // set default
    if (!out->is_transaction_id_set) {
        size_t token_size = strlen("None");
        (out->transaction_id).data = (u_char*)njt_pcalloc(pool, token_size + 1);
        js2c_malloc_check((out->transaction_id).data);
        (out->transaction_id).len = token_size;
        if (out->transaction_id.len == 0) {
            (out->transaction_id).data[0] = 0;
        }
        if (token_size > 0) {
            njt_memcpy(out->transaction_id.data, "None", token_size);
        }
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static bool parse_dynmodsecurity_locationDef_locations(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_locationDef_locations_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i] = njt_pcalloc(pool, sizeof(dynmodsecurity_locationDef_t));
        memset(((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i], 0, sizeof(dynmodsecurity_locationDef_locations_item_t));
        if (parse_dynmodsecurity_locationDef(pool, parse_state, ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i], err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynmodsecurity_locationDef(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_locationDef_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "location")) {
            js2c_check_field_set(out->is_location_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "location";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->location))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->location))->data);
            ((&out->location))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->location), 0, ((&out->location))->len, err_ret)) {
                return true;
            }
            out->is_location_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "modsecurity")) {
            js2c_check_field_set(out->is_modsecurity_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "modsecurity";
            js2c_null_check();
            out->modsecurity = njt_pcalloc(pool, sizeof(dynmodsecurity_locationDef_modsecurity_t));
            js2c_malloc_check(out->modsecurity);
            memset(out->modsecurity, 0, sizeof(dynmodsecurity_locationDef_modsecurity_t));

            if (parse_dynmodsecurity_locationDef_modsecurity(pool, parse_state, (out->modsecurity), err_ret)) {
                return true;
            }
            out->is_modsecurity_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "locations")) {
            js2c_check_field_set(out->is_locations_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "locations";
            js2c_null_check();
            out->locations = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynmodsecurity_locationDef_locations_item_t*));
            js2c_malloc_check(out->locations);

            if (parse_dynmodsecurity_locationDef_locations(pool, parse_state, (out->locations), err_ret)) {
                return true;
            }
            out->is_locations_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_location_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': location", parse_state->current_key);
        return true;
    }
    // set default
    if (!out->is_modsecurity_set) {
        out->modsecurity = NULL;
    }
    // set default
    if (!out->is_locations_set) {
        out->locations = njt_pcalloc(pool, sizeof(njt_array_t));
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_dynmodsecurity_locationDef_location(njt_pool_t *pool, dynmodsecurity_locationDef_location_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_locationDef_modsecurity_enable(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_enable_t *out, size_t *length, njt_int_t flags) {
    if (*out) {
        *length += 4; // "true"
    } else {
        *length += 5; // "false"
    }
}

static void get_json_length_dynmodsecurity_locationDef_modsecurity_rules_file(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_rules_file_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_locationDef_modsecurity_rules_remote(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_rules_remote_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_locationDef_modsecurity_rules(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_rules_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_locationDef_modsecurity_transaction_id(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_transaction_id_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_locationDef_modsecurity(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_enable_set ? 0 : 1;
    if (omit == 0) {
        *length += (6 + 3); // "enable": 
        get_json_length_dynmodsecurity_locationDef_modsecurity_enable(pool, (&out->enable), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_rules_file_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->rules_file.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (10 + 3); // "rules_file": 
        get_json_length_dynmodsecurity_locationDef_modsecurity_rules_file(pool, (&out->rules_file), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_rules_remote_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->rules_remote.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (12 + 3); // "rules_remote": 
        get_json_length_dynmodsecurity_locationDef_modsecurity_rules_remote(pool, (&out->rules_remote), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_rules_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->rules.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (5 + 3); // "rules": 
        get_json_length_dynmodsecurity_locationDef_modsecurity_rules(pool, (&out->rules), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_transaction_id_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->transaction_id.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (14 + 3); // "transaction_id": 
        get_json_length_dynmodsecurity_locationDef_modsecurity_transaction_id(pool, (&out->transaction_id), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

static void get_json_length_dynmodsecurity_locationDef_locations_item(njt_pool_t *pool, dynmodsecurity_locationDef_locations_item_t *out, size_t *length, njt_int_t flags) {
    get_json_length_dynmodsecurity_locationDef(pool, out, length, flags);
}

static void get_json_length_dynmodsecurity_locationDef_locations(njt_pool_t *pool, dynmodsecurity_locationDef_locations_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_dynmodsecurity_locationDef_locations_item(pool, ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynmodsecurity_locationDef(njt_pool_t *pool, dynmodsecurity_locationDef_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_location_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->location.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (8 + 3); // "location": 
        get_json_length_dynmodsecurity_locationDef_location(pool, (&out->location), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_modsecurity_set ? 0 : 1;
    omit = (flags & OMIT_NULL_OBJ) && (out->modsecurity) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (11 + 3); // "modsecurity": 
        get_json_length_dynmodsecurity_locationDef_modsecurity(pool, (out->modsecurity), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_locations_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->locations) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (9 + 3); // "locations": 
        get_json_length_dynmodsecurity_locationDef_locations(pool, (out->locations), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

dynmodsecurity_locationDef_modsecurity_enable_t get_dynmodsecurity_locationDef_modsecurity_enable(dynmodsecurity_locationDef_modsecurity_t *out) {
    return out->enable;
}

dynmodsecurity_locationDef_modsecurity_rules_file_t* get_dynmodsecurity_locationDef_modsecurity_rules_file(dynmodsecurity_locationDef_modsecurity_t *out) {
    return &out->rules_file;
}

dynmodsecurity_locationDef_modsecurity_rules_remote_t* get_dynmodsecurity_locationDef_modsecurity_rules_remote(dynmodsecurity_locationDef_modsecurity_t *out) {
    return &out->rules_remote;
}

dynmodsecurity_locationDef_modsecurity_rules_t* get_dynmodsecurity_locationDef_modsecurity_rules(dynmodsecurity_locationDef_modsecurity_t *out) {
    return &out->rules;
}

dynmodsecurity_locationDef_modsecurity_transaction_id_t* get_dynmodsecurity_locationDef_modsecurity_transaction_id(dynmodsecurity_locationDef_modsecurity_t *out) {
    return &out->transaction_id;
}
dynmodsecurity_locationDef_locations_item_t* get_dynmodsecurity_locationDef_locations_item(dynmodsecurity_locationDef_locations_t *out, size_t idx) {
    return ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[idx];

}

dynmodsecurity_locationDef_location_t* get_dynmodsecurity_locationDef_location(dynmodsecurity_locationDef_t *out) {
    return &out->location;
}

dynmodsecurity_locationDef_modsecurity_t* get_dynmodsecurity_locationDef_modsecurity(dynmodsecurity_locationDef_t *out) {
    return out->modsecurity;
}

dynmodsecurity_locationDef_locations_t* get_dynmodsecurity_locationDef_locations(dynmodsecurity_locationDef_t *out) {
    return out->locations;
}
void set_dynmodsecurity_locationDef_location(dynmodsecurity_locationDef_t* obj, dynmodsecurity_locationDef_location_t* field) {
    njt_memcpy(&obj->location, field, sizeof(njt_str_t));
    obj->is_location_set = 1;
}
void set_dynmodsecurity_locationDef_modsecurity_enable(dynmodsecurity_locationDef_modsecurity_t* obj, dynmodsecurity_locationDef_modsecurity_enable_t field) {
    obj->enable = field;
    obj->is_enable_set = 1;
}
void set_dynmodsecurity_locationDef_modsecurity_rules_file(dynmodsecurity_locationDef_modsecurity_t* obj, dynmodsecurity_locationDef_modsecurity_rules_file_t* field) {
    njt_memcpy(&obj->rules_file, field, sizeof(njt_str_t));
    obj->is_rules_file_set = 1;
}
void set_dynmodsecurity_locationDef_modsecurity_rules_remote(dynmodsecurity_locationDef_modsecurity_t* obj, dynmodsecurity_locationDef_modsecurity_rules_remote_t* field) {
    njt_memcpy(&obj->rules_remote, field, sizeof(njt_str_t));
    obj->is_rules_remote_set = 1;
}
void set_dynmodsecurity_locationDef_modsecurity_rules(dynmodsecurity_locationDef_modsecurity_t* obj, dynmodsecurity_locationDef_modsecurity_rules_t* field) {
    njt_memcpy(&obj->rules, field, sizeof(njt_str_t));
    obj->is_rules_set = 1;
}
void set_dynmodsecurity_locationDef_modsecurity_transaction_id(dynmodsecurity_locationDef_modsecurity_t* obj, dynmodsecurity_locationDef_modsecurity_transaction_id_t* field) {
    njt_memcpy(&obj->transaction_id, field, sizeof(njt_str_t));
    obj->is_transaction_id_set = 1;
}
dynmodsecurity_locationDef_modsecurity_t* create_dynmodsecurity_locationDef_modsecurity(njt_pool_t *pool) {
    dynmodsecurity_locationDef_modsecurity_t* out = njt_pcalloc(pool, sizeof(dynmodsecurity_locationDef_modsecurity_t));
    return out;
}
void set_dynmodsecurity_locationDef_modsecurity(dynmodsecurity_locationDef_t* obj, dynmodsecurity_locationDef_modsecurity_t* field) {
    obj->modsecurity = field;
    obj->is_modsecurity_set = 1;
}
int add_item_dynmodsecurity_locationDef_locations(dynmodsecurity_locationDef_locations_t *src, dynmodsecurity_locationDef_locations_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynmodsecurity_locationDef_locations_t* create_dynmodsecurity_locationDef_locations(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(dynmodsecurity_locationDef_locations_item_t*));
}
void set_dynmodsecurity_locationDef_locations(dynmodsecurity_locationDef_t* obj, dynmodsecurity_locationDef_locations_t* field) {
    obj->locations = field;
    obj->is_locations_set = 1;
}
dynmodsecurity_locationDef_t* create_dynmodsecurity_locationDef(njt_pool_t *pool) {
    dynmodsecurity_locationDef_t* out = njt_pcalloc(pool, sizeof(dynmodsecurity_locationDef_t));
    return out;
}

static void to_oneline_json_dynmodsecurity_locationDef_location(njt_pool_t *pool, dynmodsecurity_locationDef_location_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_locationDef_modsecurity_enable(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_enable_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out) {
        njt_sprintf(cur, "true");
        buf->len += 4;
    } else {
        njt_sprintf(cur, "false");
        buf->len += 5;
    }
}

static void to_oneline_json_dynmodsecurity_locationDef_modsecurity_rules_file(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_rules_file_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_locationDef_modsecurity_rules_remote(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_rules_remote_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_locationDef_modsecurity_rules(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_rules_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_locationDef_modsecurity_transaction_id(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_transaction_id_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_locationDef_modsecurity(njt_pool_t *pool, dynmodsecurity_locationDef_modsecurity_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_enable_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"enable\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_modsecurity_enable(pool, (&out->enable), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_rules_file_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->rules_file.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"rules_file\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_modsecurity_rules_file(pool, (&out->rules_file), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_rules_remote_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->rules_remote.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"rules_remote\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_modsecurity_rules_remote(pool, (&out->rules_remote), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_rules_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->rules.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"rules\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_modsecurity_rules(pool, (&out->rules), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_transaction_id_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->transaction_id.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"transaction_id\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_modsecurity_transaction_id(pool, (&out->transaction_id), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
// to_oneline_json_REF

static void to_oneline_json_dynmodsecurity_locationDef_locations_item(njt_pool_t *pool, dynmodsecurity_locationDef_locations_item_t *out, njt_str_t* buf, njt_int_t flags) {
    to_oneline_json_dynmodsecurity_locationDef(pool, out, buf, flags);
}

static void to_oneline_json_dynmodsecurity_locationDef_locations(njt_pool_t *pool, dynmodsecurity_locationDef_locations_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_dynmodsecurity_locationDef_locations_item(pool, ((dynmodsecurity_locationDef_locations_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynmodsecurity_locationDef(njt_pool_t *pool, dynmodsecurity_locationDef_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_location_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->location.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"location\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_location(pool, (&out->location), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_modsecurity_set ? 0 : 1;
    omit = (flags & OMIT_NULL_OBJ) && (out->modsecurity) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"modsecurity\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_modsecurity(pool, (out->modsecurity), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_locations_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->locations) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"locations\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_locationDef_locations(pool, (out->locations), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}

static bool parse_dynmodsecurity_servers_item_listens(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_servers_item_listens_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
        ((&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
        js2c_malloc_check(((&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]))->data);
        ((&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]))->len = token_size;
        if (builtin_parse_string(pool, parse_state, (&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]), 1, ((&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]))->len, err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynmodsecurity_servers_item_serverNames(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_servers_item_serverNames_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
        ((&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
        js2c_malloc_check(((&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]))->data);
        ((&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]))->len = token_size;
        if (builtin_parse_string(pool, parse_state, (&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]), 0, ((&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]))->len, err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynmodsecurity_servers_item_locations(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_servers_item_locations_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i] = njt_pcalloc(pool, sizeof(dynmodsecurity_locationDef_t));
        memset(((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i], 0, sizeof(dynmodsecurity_servers_item_locations_item_t));
        if (parse_dynmodsecurity_locationDef(pool, parse_state, ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i], err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynmodsecurity_servers_item(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_servers_item_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "listens")) {
            js2c_check_field_set(out->is_listens_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "listens";
            out->listens = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(njt_str_t));
            js2c_malloc_check(out->listens);

            if (parse_dynmodsecurity_servers_item_listens(pool, parse_state, (out->listens), err_ret)) {
                return true;
            }
            out->is_listens_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "serverNames")) {
            js2c_check_field_set(out->is_serverNames_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "serverNames";
            out->serverNames = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(njt_str_t));
            js2c_malloc_check(out->serverNames);

            if (parse_dynmodsecurity_servers_item_serverNames(pool, parse_state, (out->serverNames), err_ret)) {
                return true;
            }
            out->is_serverNames_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "locations")) {
            js2c_check_field_set(out->is_locations_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "locations";
            out->locations = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynmodsecurity_servers_item_locations_item_t*));
            js2c_malloc_check(out->locations);

            if (parse_dynmodsecurity_servers_item_locations(pool, parse_state, (out->locations), err_ret)) {
                return true;
            }
            out->is_locations_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_listens_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': listens", parse_state->current_key);
        return true;
    }
    if (!out->is_serverNames_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': serverNames", parse_state->current_key);
        return true;
    }
    if (!out->is_locations_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': locations", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static bool parse_dynmodsecurity_servers(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_servers_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        ((dynmodsecurity_servers_item_t**)out->elts)[i] = njt_pcalloc(pool, sizeof(dynmodsecurity_servers_item_t));
        memset(((dynmodsecurity_servers_item_t**)out->elts)[i], 0, sizeof(dynmodsecurity_servers_item_t));
        if (parse_dynmodsecurity_servers_item(pool, parse_state, ((dynmodsecurity_servers_item_t**)out->elts)[i], err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynmodsecurity(njt_pool_t *pool, parse_state_t *parse_state, dynmodsecurity_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "servers")) {
            js2c_check_field_set(out->is_servers_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "servers";
            out->servers = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynmodsecurity_servers_item_t*));
            js2c_malloc_check(out->servers);

            if (parse_dynmodsecurity_servers(pool, parse_state, (out->servers), err_ret)) {
                return true;
            }
            out->is_servers_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_servers_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': servers", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_dynmodsecurity_servers_item_listens_item(njt_pool_t *pool, dynmodsecurity_servers_item_listens_item_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_servers_item_listens(njt_pool_t *pool, dynmodsecurity_servers_item_listens_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_dynmodsecurity_servers_item_listens_item(pool, (&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]), length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynmodsecurity_servers_item_serverNames_item(njt_pool_t *pool, dynmodsecurity_servers_item_serverNames_item_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_dynmodsecurity_servers_item_serverNames(njt_pool_t *pool, dynmodsecurity_servers_item_serverNames_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_dynmodsecurity_servers_item_serverNames_item(pool, (&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]), length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynmodsecurity_servers_item_locations_item(njt_pool_t *pool, dynmodsecurity_servers_item_locations_item_t *out, size_t *length, njt_int_t flags) {
    get_json_length_dynmodsecurity_locationDef(pool, out, length, flags);
}

static void get_json_length_dynmodsecurity_servers_item_locations(njt_pool_t *pool, dynmodsecurity_servers_item_locations_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_dynmodsecurity_servers_item_locations_item(pool, ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynmodsecurity_servers_item(njt_pool_t *pool, dynmodsecurity_servers_item_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_listens_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->listens) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (7 + 3); // "listens": 
        get_json_length_dynmodsecurity_servers_item_listens(pool, (out->listens), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_serverNames_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->serverNames) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (11 + 3); // "serverNames": 
        get_json_length_dynmodsecurity_servers_item_serverNames(pool, (out->serverNames), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_locations_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->locations) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (9 + 3); // "locations": 
        get_json_length_dynmodsecurity_servers_item_locations(pool, (out->locations), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

static void get_json_length_dynmodsecurity_servers(njt_pool_t *pool, dynmodsecurity_servers_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((dynmodsecurity_servers_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_dynmodsecurity_servers_item(pool, ((dynmodsecurity_servers_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynmodsecurity(njt_pool_t *pool, dynmodsecurity_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_servers_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->servers) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (7 + 3); // "servers": 
        get_json_length_dynmodsecurity_servers(pool, (out->servers), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}
dynmodsecurity_servers_item_listens_item_t* get_dynmodsecurity_servers_item_listens_item(dynmodsecurity_servers_item_listens_t *out, size_t idx) {
    return &((dynmodsecurity_servers_item_listens_item_t*)out->elts)[idx];

}
dynmodsecurity_servers_item_serverNames_item_t* get_dynmodsecurity_servers_item_serverNames_item(dynmodsecurity_servers_item_serverNames_t *out, size_t idx) {
    return &((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[idx];

}
dynmodsecurity_servers_item_locations_item_t* get_dynmodsecurity_servers_item_locations_item(dynmodsecurity_servers_item_locations_t *out, size_t idx) {
    return ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[idx];

}

dynmodsecurity_servers_item_listens_t* get_dynmodsecurity_servers_item_listens(dynmodsecurity_servers_item_t *out) {
    return out->listens;
}

dynmodsecurity_servers_item_serverNames_t* get_dynmodsecurity_servers_item_serverNames(dynmodsecurity_servers_item_t *out) {
    return out->serverNames;
}

dynmodsecurity_servers_item_locations_t* get_dynmodsecurity_servers_item_locations(dynmodsecurity_servers_item_t *out) {
    return out->locations;
}
dynmodsecurity_servers_item_t* get_dynmodsecurity_servers_item(dynmodsecurity_servers_t *out, size_t idx) {
    return ((dynmodsecurity_servers_item_t**)out->elts)[idx];

}

dynmodsecurity_servers_t* get_dynmodsecurity_servers(dynmodsecurity_t *out) {
    return out->servers;
}
int add_item_dynmodsecurity_servers_item_listens(dynmodsecurity_servers_item_listens_t *src, dynmodsecurity_servers_item_listens_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, item, src->size);
    return NJT_OK;
}

dynmodsecurity_servers_item_listens_t* create_dynmodsecurity_servers_item_listens(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(dynmodsecurity_servers_item_listens_item_t));
}
void set_dynmodsecurity_servers_item_listens(dynmodsecurity_servers_item_t* obj, dynmodsecurity_servers_item_listens_t* field) {
    obj->listens = field;
    obj->is_listens_set = 1;
}
int add_item_dynmodsecurity_servers_item_serverNames(dynmodsecurity_servers_item_serverNames_t *src, dynmodsecurity_servers_item_serverNames_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, item, src->size);
    return NJT_OK;
}

dynmodsecurity_servers_item_serverNames_t* create_dynmodsecurity_servers_item_serverNames(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(dynmodsecurity_servers_item_serverNames_item_t));
}
void set_dynmodsecurity_servers_item_serverNames(dynmodsecurity_servers_item_t* obj, dynmodsecurity_servers_item_serverNames_t* field) {
    obj->serverNames = field;
    obj->is_serverNames_set = 1;
}
int add_item_dynmodsecurity_servers_item_locations(dynmodsecurity_servers_item_locations_t *src, dynmodsecurity_servers_item_locations_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynmodsecurity_servers_item_locations_t* create_dynmodsecurity_servers_item_locations(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(dynmodsecurity_servers_item_locations_item_t*));
}
void set_dynmodsecurity_servers_item_locations(dynmodsecurity_servers_item_t* obj, dynmodsecurity_servers_item_locations_t* field) {
    obj->locations = field;
    obj->is_locations_set = 1;
}
dynmodsecurity_servers_item_t* create_dynmodsecurity_servers_item(njt_pool_t *pool) {
    dynmodsecurity_servers_item_t* out = njt_pcalloc(pool, sizeof(dynmodsecurity_servers_item_t));
    return out;
}
int add_item_dynmodsecurity_servers(dynmodsecurity_servers_t *src, dynmodsecurity_servers_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynmodsecurity_servers_t* create_dynmodsecurity_servers(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(dynmodsecurity_servers_item_t*));
}
void set_dynmodsecurity_servers(dynmodsecurity_t* obj, dynmodsecurity_servers_t* field) {
    obj->servers = field;
    obj->is_servers_set = 1;
}
dynmodsecurity_t* create_dynmodsecurity(njt_pool_t *pool) {
    dynmodsecurity_t* out = njt_pcalloc(pool, sizeof(dynmodsecurity_t));
    return out;
}

static void to_oneline_json_dynmodsecurity_servers_item_listens_item(njt_pool_t *pool, dynmodsecurity_servers_item_listens_item_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_servers_item_listens(njt_pool_t *pool, dynmodsecurity_servers_item_listens_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_dynmodsecurity_servers_item_listens_item(pool, (&((dynmodsecurity_servers_item_listens_item_t*)out->elts)[i]), buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynmodsecurity_servers_item_serverNames_item(njt_pool_t *pool, dynmodsecurity_servers_item_serverNames_item_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_dynmodsecurity_servers_item_serverNames(njt_pool_t *pool, dynmodsecurity_servers_item_serverNames_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_dynmodsecurity_servers_item_serverNames_item(pool, (&((dynmodsecurity_servers_item_serverNames_item_t*)out->elts)[i]), buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}
// to_oneline_json_REF

static void to_oneline_json_dynmodsecurity_servers_item_locations_item(njt_pool_t *pool, dynmodsecurity_servers_item_locations_item_t *out, njt_str_t* buf, njt_int_t flags) {
    to_oneline_json_dynmodsecurity_locationDef(pool, out, buf, flags);
}

static void to_oneline_json_dynmodsecurity_servers_item_locations(njt_pool_t *pool, dynmodsecurity_servers_item_locations_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_dynmodsecurity_servers_item_locations_item(pool, ((dynmodsecurity_servers_item_locations_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynmodsecurity_servers_item(njt_pool_t *pool, dynmodsecurity_servers_item_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_listens_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->listens) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"listens\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_servers_item_listens(pool, (out->listens), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_serverNames_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->serverNames) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"serverNames\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_servers_item_serverNames(pool, (out->serverNames), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_locations_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->locations) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"locations\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_servers_item_locations(pool, (out->locations), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}

static void to_oneline_json_dynmodsecurity_servers(njt_pool_t *pool, dynmodsecurity_servers_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((dynmodsecurity_servers_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_dynmodsecurity_servers_item(pool, ((dynmodsecurity_servers_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynmodsecurity(njt_pool_t *pool, dynmodsecurity_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_servers_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->servers) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"servers\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynmodsecurity_servers(pool, (out->servers), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
dynmodsecurity_t* json_parse_dynmodsecurity(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    dynmodsecurity_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(dynmodsecurity_t));;
    if (parse_dynmodsecurity(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_dynmodsecurity(njt_pool_t *pool, dynmodsecurity_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_dynmodsecurity(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_dynmodsecurity(pool, out, json_str, flags);
    return json_str;
}
