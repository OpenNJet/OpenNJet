

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_token_parser.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */


static bool parse_token_sync(njt_pool_t *pool, parse_state_t *parse_state, token_sync_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "token_key")) {
            js2c_check_field_set(out->is_token_key_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "token_key";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->token_key))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->token_key))->data);
            ((&out->token_key))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->token_key), 0, ((&out->token_key))->len, err_ret)) {
                return true;
            }
            out->is_token_key_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "token_value")) {
            js2c_check_field_set(out->is_token_value_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "token_value";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->token_value))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->token_value))->data);
            ((&out->token_value))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->token_value), 0, ((&out->token_value))->len, err_ret)) {
                return true;
            }
            out->is_token_value_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "last_seen")) {
            js2c_check_field_set(out->is_last_seen_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "last_seen";
            int64_t int_parse_tmp;
            if (builtin_parse_signed(pool, parse_state, true, false, 10, &int_parse_tmp, err_ret)) {
                return true;
            }
            *(&out->last_seen) = int_parse_tmp;
            out->is_last_seen_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "ori_ttl")) {
            js2c_check_field_set(out->is_ori_ttl_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "ori_ttl";
            int64_t int_parse_tmp;
            if (builtin_parse_signed(pool, parse_state, true, false, 10, &int_parse_tmp, err_ret)) {
                return true;
            }
            *(&out->ori_ttl) = int_parse_tmp;
            out->is_ori_ttl_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_token_key_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': token_key", parse_state->current_key);
        return true;
    }
    if (!out->is_token_value_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': token_value", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_token_sync_token_key(njt_pool_t *pool, token_sync_token_key_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_token_sync_token_value(njt_pool_t *pool, token_sync_token_value_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_token_sync_last_seen(njt_pool_t *pool, token_sync_last_seen_t *out, size_t *length, njt_int_t flags) {
    u_char str[24];
    u_char *cur;
    cur = njt_sprintf(str, "%L", *out);
    *length += cur - str;
}

static void get_json_length_token_sync_ori_ttl(njt_pool_t *pool, token_sync_ori_ttl_t *out, size_t *length, njt_int_t flags) {
    u_char str[24];
    u_char *cur;
    cur = njt_sprintf(str, "%L", *out);
    *length += cur - str;
}

static void get_json_length_token_sync(njt_pool_t *pool, token_sync_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_token_key_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->token_key.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (9 + 3); // "token_key": 
        get_json_length_token_sync_token_key(pool, (&out->token_key), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_token_value_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->token_value.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (11 + 3); // "token_value": 
        get_json_length_token_sync_token_value(pool, (&out->token_value), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_last_seen_set ? 0 : 1;
    if (omit == 0) {
        *length += (9 + 3); // "last_seen": 
        get_json_length_token_sync_last_seen(pool, (&out->last_seen), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_ori_ttl_set ? 0 : 1;
    if (omit == 0) {
        *length += (7 + 3); // "ori_ttl": 
        get_json_length_token_sync_ori_ttl(pool, (&out->ori_ttl), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

token_sync_token_key_t* get_token_sync_token_key(token_sync_t *out) {
    return &out->token_key;
}

token_sync_token_value_t* get_token_sync_token_value(token_sync_t *out) {
    return &out->token_value;
}

token_sync_last_seen_t get_token_sync_last_seen(token_sync_t *out) {
    return out->last_seen;
}

token_sync_ori_ttl_t get_token_sync_ori_ttl(token_sync_t *out) {
    return out->ori_ttl;
}
void set_token_sync_token_key(token_sync_t* obj, token_sync_token_key_t* field) {
    njt_memcpy(&obj->token_key, field, sizeof(njt_str_t));
    obj->is_token_key_set = 1;
}
void set_token_sync_token_value(token_sync_t* obj, token_sync_token_value_t* field) {
    njt_memcpy(&obj->token_value, field, sizeof(njt_str_t));
    obj->is_token_value_set = 1;
}
void set_token_sync_last_seen(token_sync_t* obj, token_sync_last_seen_t field) {
    obj->last_seen = field;
    obj->is_last_seen_set = 1;
}
void set_token_sync_ori_ttl(token_sync_t* obj, token_sync_ori_ttl_t field) {
    obj->ori_ttl = field;
    obj->is_ori_ttl_set = 1;
}
token_sync_t* create_token_sync(njt_pool_t *pool) {
    token_sync_t* out = njt_pcalloc(pool, sizeof(token_sync_t));
    return out;
}

static void to_oneline_json_token_sync_token_key(njt_pool_t *pool, token_sync_token_key_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_token_sync_token_value(njt_pool_t *pool, token_sync_token_value_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_token_sync_last_seen(njt_pool_t *pool, token_sync_last_seen_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    cur = njt_sprintf(cur, "%L", *out);
    buf->len = cur - buf->data;
}

static void to_oneline_json_token_sync_ori_ttl(njt_pool_t *pool, token_sync_ori_ttl_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    cur = njt_sprintf(cur, "%L", *out);
    buf->len = cur - buf->data;
}

static void to_oneline_json_token_sync(njt_pool_t *pool, token_sync_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_token_key_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->token_key.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"token_key\":");
        buf->len = cur - buf->data;
        to_oneline_json_token_sync_token_key(pool, (&out->token_key), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_token_value_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->token_value.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"token_value\":");
        buf->len = cur - buf->data;
        to_oneline_json_token_sync_token_value(pool, (&out->token_value), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_last_seen_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"last_seen\":");
        buf->len = cur - buf->data;
        to_oneline_json_token_sync_last_seen(pool, (&out->last_seen), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_ori_ttl_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"ori_ttl\":");
        buf->len = cur - buf->data;
        to_oneline_json_token_sync_ori_ttl(pool, (&out->ori_ttl), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
token_sync_t* json_parse_token_sync(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    token_sync_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(token_sync_t));;
    if (parse_token_sync(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_token_sync(njt_pool_t *pool, token_sync_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_token_sync(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_token_sync(pool, out, json_str, flags);
    return json_str;
}
