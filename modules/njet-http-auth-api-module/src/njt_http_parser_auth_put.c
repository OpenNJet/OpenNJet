

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_parser_auth_put.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */


static bool parse_auth_passwd_put_api(njt_pool_t *pool, parse_state_t *parse_state, auth_passwd_put_api_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "prefix")) {
            js2c_check_field_set(out->is_prefix_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "prefix";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->prefix))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->prefix))->data);
            ((&out->prefix))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->prefix), 0, ((&out->prefix))->len, err_ret)) {
                return true;
            }
            out->is_prefix_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "user_name")) {
            js2c_check_field_set(out->is_user_name_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "user_name";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->user_name))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->user_name))->data);
            ((&out->user_name))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->user_name), 0, ((&out->user_name))->len, err_ret)) {
                return true;
            }
            out->is_user_name_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "password")) {
            js2c_check_field_set(out->is_password_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "password";
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->password))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->password))->data);
            ((&out->password))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->password), 0, ((&out->password))->len, err_ret)) {
                return true;
            }
            out->is_password_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_prefix_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': prefix", parse_state->current_key);
        return true;
    }
    if (!out->is_user_name_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': user_name", parse_state->current_key);
        return true;
    }
    if (!out->is_password_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': password", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_auth_passwd_put_api_prefix(njt_pool_t *pool, auth_passwd_put_api_prefix_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_auth_passwd_put_api_user_name(njt_pool_t *pool, auth_passwd_put_api_user_name_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_auth_passwd_put_api_password(njt_pool_t *pool, auth_passwd_put_api_password_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_auth_passwd_put_api(njt_pool_t *pool, auth_passwd_put_api_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_prefix_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->prefix.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (6 + 3); // "prefix": 
        get_json_length_auth_passwd_put_api_prefix(pool, (&out->prefix), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_user_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->user_name.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (9 + 3); // "user_name": 
        get_json_length_auth_passwd_put_api_user_name(pool, (&out->user_name), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_password_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->password.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (8 + 3); // "password": 
        get_json_length_auth_passwd_put_api_password(pool, (&out->password), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

auth_passwd_put_api_prefix_t* get_auth_passwd_put_api_prefix(auth_passwd_put_api_t *out) {
    return &out->prefix;
}

auth_passwd_put_api_user_name_t* get_auth_passwd_put_api_user_name(auth_passwd_put_api_t *out) {
    return &out->user_name;
}

auth_passwd_put_api_password_t* get_auth_passwd_put_api_password(auth_passwd_put_api_t *out) {
    return &out->password;
}
void set_auth_passwd_put_api_prefix(auth_passwd_put_api_t* obj, auth_passwd_put_api_prefix_t* field) {
    njt_memcpy(&obj->prefix, field, sizeof(njt_str_t));
    obj->is_prefix_set = 1;
}
void set_auth_passwd_put_api_user_name(auth_passwd_put_api_t* obj, auth_passwd_put_api_user_name_t* field) {
    njt_memcpy(&obj->user_name, field, sizeof(njt_str_t));
    obj->is_user_name_set = 1;
}
void set_auth_passwd_put_api_password(auth_passwd_put_api_t* obj, auth_passwd_put_api_password_t* field) {
    njt_memcpy(&obj->password, field, sizeof(njt_str_t));
    obj->is_password_set = 1;
}
auth_passwd_put_api_t* create_auth_passwd_put_api(njt_pool_t *pool) {
    auth_passwd_put_api_t* out = njt_pcalloc(pool, sizeof(auth_passwd_put_api_t));
    return out;
}

static void to_oneline_json_auth_passwd_put_api_prefix(njt_pool_t *pool, auth_passwd_put_api_prefix_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_auth_passwd_put_api_user_name(njt_pool_t *pool, auth_passwd_put_api_user_name_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_auth_passwd_put_api_password(njt_pool_t *pool, auth_passwd_put_api_password_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_auth_passwd_put_api(njt_pool_t *pool, auth_passwd_put_api_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_prefix_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->prefix.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"prefix\":");
        buf->len = cur - buf->data;
        to_oneline_json_auth_passwd_put_api_prefix(pool, (&out->prefix), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_user_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->user_name.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"user_name\":");
        buf->len = cur - buf->data;
        to_oneline_json_auth_passwd_put_api_user_name(pool, (&out->user_name), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_password_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->password.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"password\":");
        buf->len = cur - buf->data;
        to_oneline_json_auth_passwd_put_api_password(pool, (&out->password), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
auth_passwd_put_api_t* json_parse_auth_passwd_put_api(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    auth_passwd_put_api_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(auth_passwd_put_api_t));;
    if (parse_auth_passwd_put_api(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_auth_passwd_put_api(njt_pool_t *pool, auth_passwd_put_api_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_auth_passwd_put_api(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_auth_passwd_put_api(pool, out, json_str, flags);
    return json_str;
}
