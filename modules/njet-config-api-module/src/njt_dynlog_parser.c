

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_dynlog_parser.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */

static bool parse_accessLog(njt_pool_t *pool, parse_state_t *parse_state, accessLog_t *out, njt_str_t* err_str); //forward decl for public definition
static void get_json_length_accessLog(accessLog_t *out, size_t *length, njt_int_t flags); //forward decl for public definition
static void to_oneline_json_accessLog(accessLog_t *out, njt_str_t *buf, njt_int_t flags); //forward decl for public definition

static bool parse_accessLog(njt_pool_t *pool, parse_state_t *parse_state, accessLog_t *out, njt_str_t *err_str) {
    njt_uint_t i;
    // malloc pool for object first

    if (check_type(pool, parse_state, JSMN_OBJECT, err_str)) {
        return true;
    }
    bool seen_path = false;
    bool seen_formatName = false;
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        if (CURRENT_TOKEN(parse_state).size > 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing separator between values in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (CURRENT_TOKEN(parse_state).size < 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing value in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (current_string_is(parse_state, "path")) {
            if (seen_path) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': path", parse_state->current_key)
                return true;
            }
            seen_path = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "path";
            out->path = njt_palloc(pool, sizeof(njt_str_t));
            if (out->path == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            // first get str len from TOKEN_STRING
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            (out->path) = njt_palloc(pool, sizeof(njt_str_t));
            if ((out->path) == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            ((out->path))->data = njt_palloc(pool, (size_t)(token_size));
            ((out->path))->len = token_size;
            if ((out->path)->len == 0) {
                ((out->path))->data = NULL;
            } else if ((out->path)->data == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            if (builtin_parse_string(pool, parse_state, (char *)((out->path))->data, 0, ((out->path))->len, err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "formatName")) {
            if (seen_formatName) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': formatName", parse_state->current_key)
                return true;
            }
            seen_formatName = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "formatName";
            out->formatName = njt_palloc(pool, sizeof(njt_str_t));
            if (out->formatName == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            // first get str len from TOKEN_STRING
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            (out->formatName) = njt_palloc(pool, sizeof(njt_str_t));
            if ((out->formatName) == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            ((out->formatName))->data = njt_palloc(pool, (size_t)(token_size));
            ((out->formatName))->len = token_size;
            if ((out->formatName)->len == 0) {
                ((out->formatName))->data = NULL;
            } else if ((out->formatName)->data == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            if (builtin_parse_string(pool, parse_state, (char *)((out->formatName))->data, 0, ((out->formatName))->len, err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!seen_path) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': path", parse_state->current_key)
        return true;
    }
    if (!seen_formatName) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': formatName", parse_state->current_key)
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_accessLog_path(accessLog_path_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}

static void get_json_length_accessLog_formatName(accessLog_formatName_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}

static void get_json_length_accessLog(accessLog_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->path) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (4 + 3); // "path": 
        get_json_length_accessLog_path((&out->path), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->formatName) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (10 + 3); // "formatName": 
        get_json_length_accessLog_formatName((&out->formatName), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

accessLog_path_t get_accessLog_path(accessLog_t *out) {
    return out->path;
}

accessLog_formatName_t get_accessLog_formatName(accessLog_t *out) {
    return out->formatName;
}
void set_accessLog_path(accessLog_t* obj, accessLog_path_t field) {
    obj->path = field;
}
void set_accessLog_formatName(accessLog_t* obj, accessLog_formatName_t field) {
    obj->formatName = field;
}
accessLog_t* create_accessLog(njt_pool_t *pool) {
    accessLog_t* out = njt_palloc(pool, sizeof(accessLog_t));
    memset(out, 0, sizeof(accessLog_t));
    return out;
}

static void to_oneline_json_accessLog_path(accessLog_path_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}

static void to_oneline_json_accessLog_formatName(accessLog_formatName_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}

static void to_oneline_json_accessLog(accessLog_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->path) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"path\":");
        buf->len = cur - buf->data;
        to_oneline_json_accessLog_path((&out->path), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->formatName) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"formatName\":");
        buf->len = cur - buf->data;
        to_oneline_json_accessLog_formatName((&out->formatName), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
static bool parse_locationDef(njt_pool_t *pool, parse_state_t *parse_state, locationDef_t *out, njt_str_t* err_str); //forward decl for public definition
static void get_json_length_locationDef(locationDef_t *out, size_t *length, njt_int_t flags); //forward decl for public definition
static void to_oneline_json_locationDef(locationDef_t *out, njt_str_t *buf, njt_int_t flags); //forward decl for public definition

static bool parse_locationDef_locations(njt_pool_t *pool, parse_state_t *parse_state, locationDef_locations_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((locationDef_locations_item_t**)out->elts)[i] = njt_palloc(pool, sizeof(locationDef_t));
        if (parse_locationDef(pool, parse_state, ((locationDef_locations_item_t**)out->elts)[i], err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_locationDef_accessLogs(njt_pool_t *pool, parse_state_t *parse_state, locationDef_accessLogs_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((locationDef_accessLogs_item_t**)out->elts)[i] = njt_palloc(pool, sizeof(accessLog_t));
        if (parse_accessLog(pool, parse_state, ((locationDef_accessLogs_item_t**)out->elts)[i], err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_locationDef(njt_pool_t *pool, parse_state_t *parse_state, locationDef_t *out, njt_str_t *err_str) {
    njt_uint_t i;
    // malloc pool for object first

    if (check_type(pool, parse_state, JSMN_OBJECT, err_str)) {
        return true;
    }
    bool seen_location = false;
    bool seen_locations = false;
    bool seen_accessLogOn = false;
    bool seen_accessLogs = false;
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        if (CURRENT_TOKEN(parse_state).size > 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing separator between values in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (CURRENT_TOKEN(parse_state).size < 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing value in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (current_string_is(parse_state, "location")) {
            if (seen_location) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': location", parse_state->current_key)
                return true;
            }
            seen_location = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "location";
            out->location = njt_palloc(pool, sizeof(njt_str_t));
            if (out->location == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            // first get str len from TOKEN_STRING
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            (out->location) = njt_palloc(pool, sizeof(njt_str_t));
            if ((out->location) == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            ((out->location))->data = njt_palloc(pool, (size_t)(token_size));
            ((out->location))->len = token_size;
            if ((out->location)->len == 0) {
                ((out->location))->data = NULL;
            } else if ((out->location)->data == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            if (builtin_parse_string(pool, parse_state, (char *)((out->location))->data, 0, ((out->location))->len, err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "locations")) {
            if (seen_locations) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': locations", parse_state->current_key)
                return true;
            }
            seen_locations = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "locations";
            out->locations = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(locationDef_locations_item_t*));
            if (out->locations == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_locationDef_locations(pool, parse_state, (out->locations), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "accessLogOn")) {
            if (seen_accessLogOn) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': accessLogOn", parse_state->current_key)
                return true;
            }
            seen_accessLogOn = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "accessLogOn";
            if (builtin_parse_bool(pool, parse_state, (&out->accessLogOn), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "accessLogs")) {
            if (seen_accessLogs) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': accessLogs", parse_state->current_key)
                return true;
            }
            seen_accessLogs = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "accessLogs";
            out->accessLogs = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(locationDef_accessLogs_item_t*));
            if (out->accessLogs == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_locationDef_accessLogs(pool, parse_state, (out->accessLogs), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!seen_location) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': location", parse_state->current_key)
        return true;
    }
    // HAHB
    if (!seen_locations) {
        out->locations = njt_palloc(pool, sizeof(njt_array_t));
        memset(out->locations, 0, sizeof(njt_array_t));
    }
    // HAHB
    if (!seen_accessLogOn) {
        out->accessLogOn = false;
    }
    // HAHB
    if (!seen_accessLogs) {
        out->accessLogs = njt_palloc(pool, sizeof(njt_array_t));
        memset(out->accessLogs, 0, sizeof(njt_array_t));
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_locationDef_location(locationDef_location_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}
// GET_JSON_LENGTH_REF

static void get_json_length_locationDef_locations_item(locationDef_locations_item_t *out, size_t *length, njt_int_t flags) {
    get_json_length_locationDef(out, length, flags);
}

static void get_json_length_locationDef_locations(locationDef_locations_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((locationDef_locations_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_locationDef_locations_item(((locationDef_locations_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_locationDef_accessLogOn(locationDef_accessLogOn_t *out, size_t *length, njt_int_t flags) {
    if (*out) {
        *length += 4; // "true"
    } else {
        *length += 5; // "false"
    }
}
// GET_JSON_LENGTH_REF

static void get_json_length_locationDef_accessLogs_item(locationDef_accessLogs_item_t *out, size_t *length, njt_int_t flags) {
    get_json_length_accessLog(out, length, flags);
}

static void get_json_length_locationDef_accessLogs(locationDef_accessLogs_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((locationDef_accessLogs_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_locationDef_accessLogs_item(((locationDef_accessLogs_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_locationDef(locationDef_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->location) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (8 + 3); // "location": 
        get_json_length_locationDef_location((&out->location), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->locations) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (9 + 3); // "locations": 
        get_json_length_locationDef_locations((out->locations), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if (omit == 0) {
        *length += (11 + 3); // "accessLogOn": 
        get_json_length_locationDef_accessLogOn((&out->accessLogOn), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->accessLogs) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (10 + 3); // "accessLogs": 
        get_json_length_locationDef_accessLogs((out->accessLogs), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}
// CHECK ARRAY not exceeding bounds before call this func

locationDef_locations_item_t get_locationDef_locations_item(locationDef_locations_t *out, size_t idx) {
    return *((locationDef_locations_item_t**)out->elts)[idx];
}
// CHECK ARRAY not exceeding bounds before call this func

locationDef_accessLogs_item_t get_locationDef_accessLogs_item(locationDef_accessLogs_t *out, size_t idx) {
    return *((locationDef_accessLogs_item_t**)out->elts)[idx];
}

locationDef_location_t get_locationDef_location(locationDef_t *out) {
    return out->location;
}

locationDef_locations_t* get_locationDef_locations(locationDef_t *out) {
    return out->locations;
}

locationDef_accessLogOn_t get_locationDef_accessLogOn(locationDef_t *out) {
    return out->accessLogOn;
}

locationDef_accessLogs_t* get_locationDef_accessLogs(locationDef_t *out) {
    return out->accessLogs;
}
void set_locationDef_location(locationDef_t* obj, locationDef_location_t field) {
    obj->location = field;
}
int add_item_locationDef_locations(locationDef_locations_t *src, locationDef_locations_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

locationDef_locations_t* create_locationDef_locations(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(locationDef_locations_item_t*));
}
void set_locationDef_locations(locationDef_t* obj, locationDef_locations_t* field) {
    obj->locations = field;
}
int add_item_locationDef_accessLogs(locationDef_accessLogs_t *src, locationDef_accessLogs_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

locationDef_accessLogs_t* create_locationDef_accessLogs(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(locationDef_accessLogs_item_t*));
}
void set_locationDef_accessLogs(locationDef_t* obj, locationDef_accessLogs_t* field) {
    obj->accessLogs = field;
}
locationDef_t* create_locationDef(njt_pool_t *pool) {
    locationDef_t* out = njt_palloc(pool, sizeof(locationDef_t));
    memset(out, 0, sizeof(locationDef_t));
    return out;
}

static void to_oneline_json_locationDef_location(locationDef_location_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}
// to_oneline_json_REF

static void to_oneline_json_locationDef_locations_item(locationDef_locations_item_t *out, njt_str_t* buf, njt_int_t flags) {
    to_oneline_json_locationDef(out, buf, flags);
}

static void to_oneline_json_locationDef_locations(locationDef_locations_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((locationDef_locations_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_locationDef_locations_item(((locationDef_locations_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_locationDef_accessLogOn(locationDef_accessLogOn_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out) {
        njt_sprintf(cur, "true");
        buf->len += 4;
    } else {
        njt_sprintf(cur, "false");
        buf->len += 5;
    }
}
// to_oneline_json_REF

static void to_oneline_json_locationDef_accessLogs_item(locationDef_accessLogs_item_t *out, njt_str_t* buf, njt_int_t flags) {
    to_oneline_json_accessLog(out, buf, flags);
}

static void to_oneline_json_locationDef_accessLogs(locationDef_accessLogs_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((locationDef_accessLogs_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_locationDef_accessLogs_item(((locationDef_accessLogs_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_locationDef(locationDef_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->location) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"location\":");
        buf->len = cur - buf->data;
        to_oneline_json_locationDef_location((&out->location), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->locations) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"locations\":");
        buf->len = cur - buf->data;
        to_oneline_json_locationDef_locations((out->locations), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"accessLogOn\":");
        buf->len = cur - buf->data;
        to_oneline_json_locationDef_accessLogOn((&out->accessLogOn), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->accessLogs) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"accessLogs\":");
        buf->len = cur - buf->data;
        to_oneline_json_locationDef_accessLogs((out->accessLogs), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
static bool parse_accessLogFormat(njt_pool_t *pool, parse_state_t *parse_state, accessLogFormat_t *out, njt_str_t* err_str); //forward decl for public definition
static void get_json_length_accessLogFormat(accessLogFormat_t *out, size_t *length, njt_int_t flags); //forward decl for public definition
static void to_oneline_json_accessLogFormat(accessLogFormat_t *out, njt_str_t *buf, njt_int_t flags); //forward decl for public definition

static bool parse_accessLogFormat_escape(njt_pool_t *pool, parse_state_t *parse_state, accessLogFormat_escape_t *out, njt_str_t *err_str) {
    if (check_type(pool, parse_state, JSMN_STRING, err_str)) {
        return true;
    }
    if (current_string_is(parse_state, "default")) {
        *out = ACCESSLOGFORMAT_ESCAPE_DEFAULT;
    } else if (current_string_is(parse_state, "json")) {
        *out = ACCESSLOGFORMAT_ESCAPE_JSON;
    } else if (current_string_is(parse_state, "none")) {
        *out = ACCESSLOGFORMAT_ESCAPE_NONE;
    } else {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Unknown enum value in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
        return true;
    }
    parse_state->current_token += 1;
    return false;
}


static bool parse_accessLogFormat(njt_pool_t *pool, parse_state_t *parse_state, accessLogFormat_t *out, njt_str_t *err_str) {
    njt_uint_t i;
    // malloc pool for object first

    if (check_type(pool, parse_state, JSMN_OBJECT, err_str)) {
        return true;
    }
    bool seen_name = false;
    bool seen_escape = false;
    bool seen_format = false;
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        if (CURRENT_TOKEN(parse_state).size > 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing separator between values in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (CURRENT_TOKEN(parse_state).size < 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing value in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (current_string_is(parse_state, "name")) {
            if (seen_name) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': name", parse_state->current_key)
                return true;
            }
            seen_name = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "name";
            out->name = njt_palloc(pool, sizeof(njt_str_t));
            if (out->name == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            // first get str len from TOKEN_STRING
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            (out->name) = njt_palloc(pool, sizeof(njt_str_t));
            if ((out->name) == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            ((out->name))->data = njt_palloc(pool, (size_t)(token_size));
            ((out->name))->len = token_size;
            if ((out->name)->len == 0) {
                ((out->name))->data = NULL;
            } else if ((out->name)->data == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            if (builtin_parse_string(pool, parse_state, (char *)((out->name))->data, 0, ((out->name))->len, err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "escape")) {
            if (seen_escape) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': escape", parse_state->current_key)
                return true;
            }
            seen_escape = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "escape";
            if (parse_accessLogFormat_escape(pool, parse_state, (&out->escape), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "format")) {
            if (seen_format) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': format", parse_state->current_key)
                return true;
            }
            seen_format = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "format";
            out->format = njt_palloc(pool, sizeof(njt_str_t));
            if (out->format == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            // first get str len from TOKEN_STRING
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            (out->format) = njt_palloc(pool, sizeof(njt_str_t));
            if ((out->format) == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            ((out->format))->data = njt_palloc(pool, (size_t)(token_size));
            ((out->format))->len = token_size;
            if ((out->format)->len == 0) {
                ((out->format))->data = NULL;
            } else if ((out->format)->data == NULL) {
                // TODO LOG_ERROR
                return true;
            }
            if (builtin_parse_string(pool, parse_state, (char *)((out->format))->data, 0, ((out->format))->len, err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!seen_name) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': name", parse_state->current_key)
        return true;
    }
    if (!seen_format) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': format", parse_state->current_key)
        return true;
    }
    // HAHB
    if (!seen_escape) {
        out->escape = ACCESSLOGFORMAT_ESCAPE_DEFAULT;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_accessLogFormat_name(accessLogFormat_name_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}
// BEGIN GET_JSON_LENGTH ENUM

static void get_json_length_accessLogFormat_escape(accessLogFormat_escape_t *out, size_t *length, njt_int_t flags) {
    if (*out == ACCESSLOGFORMAT_ESCAPE_DEFAULT) {
        // "default"
        *length += 7 + 2;
        return;
    }
    if (*out == ACCESSLOGFORMAT_ESCAPE_JSON) {
        // "json"
        *length += 4 + 2;
        return;
    }
    if (*out == ACCESSLOGFORMAT_ESCAPE_NONE) {
        // "none"
        *length += 4 + 2;
        return;
    }
}

static void get_json_length_accessLogFormat_format(accessLogFormat_format_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}

static void get_json_length_accessLogFormat(accessLogFormat_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->name) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (4 + 3); // "name": 
        get_json_length_accessLogFormat_name((&out->name), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if (omit == 0) {
        *length += (6 + 3); // "escape": 
        get_json_length_accessLogFormat_escape((&out->escape), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->format) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (6 + 3); // "format": 
        get_json_length_accessLogFormat_format((&out->format), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

accessLogFormat_name_t get_accessLogFormat_name(accessLogFormat_t *out) {
    return out->name;
}

accessLogFormat_escape_t get_accessLogFormat_escape(accessLogFormat_t *out) {
    return out->escape;
}

accessLogFormat_format_t get_accessLogFormat_format(accessLogFormat_t *out) {
    return out->format;
}
void set_accessLogFormat_name(accessLogFormat_t* obj, accessLogFormat_name_t field) {
    obj->name = field;
}
void set_accessLogFormat_format(accessLogFormat_t* obj, accessLogFormat_format_t field) {
    obj->format = field;
}
accessLogFormat_t* create_accessLogFormat(njt_pool_t *pool) {
    accessLogFormat_t* out = njt_palloc(pool, sizeof(accessLogFormat_t));
    memset(out, 0, sizeof(accessLogFormat_t));
    return out;
}

static void to_oneline_json_accessLogFormat_name(accessLogFormat_name_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}

static void to_oneline_json_accessLogFormat_escape(accessLogFormat_escape_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out == ACCESSLOGFORMAT_ESCAPE_DEFAULT) {
        cur = njt_sprintf(cur, "\"default\"");
        buf->len += 7 + 2;
        return;
    }
    if (*out == ACCESSLOGFORMAT_ESCAPE_JSON) {
        cur = njt_sprintf(cur, "\"json\"");
        buf->len += 4 + 2;
        return;
    }
    if (*out == ACCESSLOGFORMAT_ESCAPE_NONE) {
        cur = njt_sprintf(cur, "\"none\"");
        buf->len += 4 + 2;
        return;
    }
}

static void to_oneline_json_accessLogFormat_format(accessLogFormat_format_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}

static void to_oneline_json_accessLogFormat(accessLogFormat_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->name) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"name\":");
        buf->len = cur - buf->data;
        to_oneline_json_accessLogFormat_name((&out->name), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"escape\":");
        buf->len = cur - buf->data;
        to_oneline_json_accessLogFormat_escape((&out->escape), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_STR) && (out->format) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"format\":");
        buf->len = cur - buf->data;
        to_oneline_json_accessLogFormat_format((&out->format), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}

static bool parse_dynlog_servers_item_listens(njt_pool_t *pool, parse_state_t *parse_state, dynlog_servers_item_listens_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    if (n < 1) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Array '%s' too small. Length: %i. Minimum length: 1.", parse_state->current_key, n)
        return true;
    }
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((dynlog_servers_item_listens_item_t*)out->elts)[i] = njt_palloc(pool, sizeof(njt_str_t));
        // first get str len from TOKEN_STRING
        int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
        ((dynlog_servers_item_listens_item_t*)out->elts)[i] = njt_palloc(pool, sizeof(njt_str_t));
        if (((dynlog_servers_item_listens_item_t*)out->elts)[i] == NULL) {
            // TODO LOG_ERROR
            return true;
        }
        (((dynlog_servers_item_listens_item_t*)out->elts)[i])->data = njt_palloc(pool, (size_t)(token_size));
        (((dynlog_servers_item_listens_item_t*)out->elts)[i])->len = token_size;
        if (((dynlog_servers_item_listens_item_t*)out->elts)[i]->len == 0) {
            (((dynlog_servers_item_listens_item_t*)out->elts)[i])->data = NULL;
        } else if (((dynlog_servers_item_listens_item_t*)out->elts)[i]->data == NULL) {
            // TODO LOG_ERROR
            return true;
        }
        if (builtin_parse_string(pool, parse_state, (char *)(((dynlog_servers_item_listens_item_t*)out->elts)[i])->data, 1, (((dynlog_servers_item_listens_item_t*)out->elts)[i])->len, err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynlog_servers_item_serverNames(njt_pool_t *pool, parse_state_t *parse_state, dynlog_servers_item_serverNames_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((dynlog_servers_item_serverNames_item_t*)out->elts)[i] = njt_palloc(pool, sizeof(njt_str_t));
        // first get str len from TOKEN_STRING
        int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
        ((dynlog_servers_item_serverNames_item_t*)out->elts)[i] = njt_palloc(pool, sizeof(njt_str_t));
        if (((dynlog_servers_item_serverNames_item_t*)out->elts)[i] == NULL) {
            // TODO LOG_ERROR
            return true;
        }
        (((dynlog_servers_item_serverNames_item_t*)out->elts)[i])->data = njt_palloc(pool, (size_t)(token_size));
        (((dynlog_servers_item_serverNames_item_t*)out->elts)[i])->len = token_size;
        if (((dynlog_servers_item_serverNames_item_t*)out->elts)[i]->len == 0) {
            (((dynlog_servers_item_serverNames_item_t*)out->elts)[i])->data = NULL;
        } else if (((dynlog_servers_item_serverNames_item_t*)out->elts)[i]->data == NULL) {
            // TODO LOG_ERROR
            return true;
        }
        if (builtin_parse_string(pool, parse_state, (char *)(((dynlog_servers_item_serverNames_item_t*)out->elts)[i])->data, 0, (((dynlog_servers_item_serverNames_item_t*)out->elts)[i])->len, err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynlog_servers_item_locations(njt_pool_t *pool, parse_state_t *parse_state, dynlog_servers_item_locations_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((dynlog_servers_item_locations_item_t**)out->elts)[i] = njt_palloc(pool, sizeof(locationDef_t));
        if (parse_locationDef(pool, parse_state, ((dynlog_servers_item_locations_item_t**)out->elts)[i], err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynlog_servers_item(njt_pool_t *pool, parse_state_t *parse_state, dynlog_servers_item_t *out, njt_str_t *err_str) {
    njt_uint_t i;
    // malloc pool for object first

    if (check_type(pool, parse_state, JSMN_OBJECT, err_str)) {
        return true;
    }
    bool seen_listens = false;
    bool seen_serverNames = false;
    bool seen_locations = false;
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        if (CURRENT_TOKEN(parse_state).size > 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing separator between values in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (CURRENT_TOKEN(parse_state).size < 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing value in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (current_string_is(parse_state, "listens")) {
            if (seen_listens) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': listens", parse_state->current_key)
                return true;
            }
            seen_listens = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "listens";
            out->listens = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynlog_servers_item_listens_item_t*));
            if (out->listens == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_dynlog_servers_item_listens(pool, parse_state, (out->listens), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "serverNames")) {
            if (seen_serverNames) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': serverNames", parse_state->current_key)
                return true;
            }
            seen_serverNames = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "serverNames";
            out->serverNames = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynlog_servers_item_serverNames_item_t*));
            if (out->serverNames == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_dynlog_servers_item_serverNames(pool, parse_state, (out->serverNames), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "locations")) {
            if (seen_locations) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': locations", parse_state->current_key)
                return true;
            }
            seen_locations = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "locations";
            out->locations = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynlog_servers_item_locations_item_t*));
            if (out->locations == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_dynlog_servers_item_locations(pool, parse_state, (out->locations), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!seen_listens) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': listens", parse_state->current_key)
        return true;
    }
    if (!seen_serverNames) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': serverNames", parse_state->current_key)
        return true;
    }
    if (!seen_locations) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': locations", parse_state->current_key)
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static bool parse_dynlog_servers(njt_pool_t *pool, parse_state_t *parse_state, dynlog_servers_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((dynlog_servers_item_t**)out->elts)[i] = njt_palloc(pool, sizeof(dynlog_servers_item_t));
        if (parse_dynlog_servers_item(pool, parse_state, ((dynlog_servers_item_t**)out->elts)[i], err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynlog_accessLogFormats(njt_pool_t *pool, parse_state_t *parse_state, dynlog_accessLogFormats_t *out, njt_str_t *err_str) {
    int i;
    if (check_type(pool, parse_state, JSMN_ARRAY, err_str)) {
        return true;
    }
    const int n = parse_state->tokens[parse_state->current_token].size;
    // memory has been allocate in njt_array_create.
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        // TODO CHECK write later
        ((dynlog_accessLogFormats_item_t**)out->elts)[i] = njt_palloc(pool, sizeof(accessLogFormat_t));
        if (parse_accessLogFormat(pool, parse_state, ((dynlog_accessLogFormats_item_t**)out->elts)[i], err_str)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_dynlog(njt_pool_t *pool, parse_state_t *parse_state, dynlog_t *out, njt_str_t *err_str) {
    njt_uint_t i;
    // malloc pool for object first

    if (check_type(pool, parse_state, JSMN_OBJECT, err_str)) {
        return true;
    }
    bool seen_servers = false;
    bool seen_accessLogFormats = false;
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        if (CURRENT_TOKEN(parse_state).size > 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing separator between values in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (CURRENT_TOKEN(parse_state).size < 1) {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing value in '%s', after key: %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
        if (current_string_is(parse_state, "servers")) {
            if (seen_servers) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': servers", parse_state->current_key)
                return true;
            }
            seen_servers = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "servers";
            out->servers = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynlog_servers_item_t*));
            if (out->servers == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_dynlog_servers(pool, parse_state, (out->servers), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "accessLogFormats")) {
            if (seen_accessLogFormats) {
                LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Duplicate field definition in '%s': accessLogFormats", parse_state->current_key)
                return true;
            }
            seen_accessLogFormats = true;
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "accessLogFormats";
            out->accessLogFormats = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(dynlog_accessLogFormats_item_t*));
            if (out->accessLogFormats == NULL) {
                LOG_ERROR_JSON_PARSE(0, "Failed to allocate memory from %s.", "pool")
            }
            if (parse_dynlog_accessLogFormats(pool, parse_state, (out->accessLogFormats), err_str)) {
                return true;
            }
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state))
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!seen_servers) {
        LOG_ERROR_JSON_PARSE(CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': servers", parse_state->current_key)
        return true;
    }
    // HAHB
    if (!seen_accessLogFormats) {
        out->accessLogFormats = njt_palloc(pool, sizeof(njt_array_t));
        memset(out->accessLogFormats, 0, sizeof(njt_array_t));
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static void get_json_length_dynlog_servers_item_listens_item(dynlog_servers_item_listens_item_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}

static void get_json_length_dynlog_servers_item_listens(dynlog_servers_item_listens_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_STR) && (&((dynlog_servers_item_listens_item_t*)out->elts)[i]) == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_dynlog_servers_item_listens_item((&((dynlog_servers_item_listens_item_t*)out->elts)[i]), length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynlog_servers_item_serverNames_item(dynlog_servers_item_serverNames_item_t *out, size_t *length, njt_int_t flags) {
    *length += (*out)->len + 2; //  "str" 
}

static void get_json_length_dynlog_servers_item_serverNames(dynlog_servers_item_serverNames_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_STR) && (&((dynlog_servers_item_serverNames_item_t*)out->elts)[i]) == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_dynlog_servers_item_serverNames_item((&((dynlog_servers_item_serverNames_item_t*)out->elts)[i]), length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}
// GET_JSON_LENGTH_REF

static void get_json_length_dynlog_servers_item_locations_item(dynlog_servers_item_locations_item_t *out, size_t *length, njt_int_t flags) {
    get_json_length_locationDef(out, length, flags);
}

static void get_json_length_dynlog_servers_item_locations(dynlog_servers_item_locations_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((dynlog_servers_item_locations_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_dynlog_servers_item_locations_item(((dynlog_servers_item_locations_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynlog_servers_item(dynlog_servers_item_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->listens) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (7 + 3); // "listens": 
        get_json_length_dynlog_servers_item_listens((out->listens), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->serverNames) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (11 + 3); // "serverNames": 
        get_json_length_dynlog_servers_item_serverNames((out->serverNames), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->locations) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (9 + 3); // "locations": 
        get_json_length_dynlog_servers_item_locations((out->locations), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

static void get_json_length_dynlog_servers(dynlog_servers_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((dynlog_servers_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_dynlog_servers_item(((dynlog_servers_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}
// GET_JSON_LENGTH_REF

static void get_json_length_dynlog_accessLogFormats_item(dynlog_accessLogFormats_item_t *out, size_t *length, njt_int_t flags) {
    get_json_length_accessLogFormat(out, length, flags);
}

static void get_json_length_dynlog_accessLogFormats(dynlog_accessLogFormats_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((dynlog_accessLogFormats_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            get_json_length_dynlog_accessLogFormats_item(((dynlog_accessLogFormats_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_dynlog(dynlog_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->servers) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (7 + 3); // "servers": 
        get_json_length_dynlog_servers((out->servers), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->accessLogFormats) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        *length += (16 + 3); // "accessLogFormats": 
        get_json_length_dynlog_accessLogFormats((out->accessLogFormats), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}
// CHECK ARRAY not exceeding bounds before call this func

dynlog_servers_item_listens_item_t get_dynlog_servers_item_listens_item(dynlog_servers_item_listens_t *out, size_t idx) {
    return ((dynlog_servers_item_listens_item_t*)out->elts)[idx];
}
// CHECK ARRAY not exceeding bounds before call this func

dynlog_servers_item_serverNames_item_t get_dynlog_servers_item_serverNames_item(dynlog_servers_item_serverNames_t *out, size_t idx) {
    return ((dynlog_servers_item_serverNames_item_t*)out->elts)[idx];
}
// CHECK ARRAY not exceeding bounds before call this func

dynlog_servers_item_locations_item_t get_dynlog_servers_item_locations_item(dynlog_servers_item_locations_t *out, size_t idx) {
    return *((dynlog_servers_item_locations_item_t**)out->elts)[idx];
}

dynlog_servers_item_listens_t* get_dynlog_servers_item_listens(dynlog_servers_item_t *out) {
    return out->listens;
}

dynlog_servers_item_serverNames_t* get_dynlog_servers_item_serverNames(dynlog_servers_item_t *out) {
    return out->serverNames;
}

dynlog_servers_item_locations_t* get_dynlog_servers_item_locations(dynlog_servers_item_t *out) {
    return out->locations;
}
// CHECK ARRAY not exceeding bounds before call this func

dynlog_servers_item_t get_dynlog_servers_item(dynlog_servers_t *out, size_t idx) {
    return *((dynlog_servers_item_t**)out->elts)[idx];
}
// CHECK ARRAY not exceeding bounds before call this func

dynlog_accessLogFormats_item_t get_dynlog_accessLogFormats_item(dynlog_accessLogFormats_t *out, size_t idx) {
    return *((dynlog_accessLogFormats_item_t**)out->elts)[idx];
}

dynlog_servers_t* get_dynlog_servers(dynlog_t *out) {
    return out->servers;
}

dynlog_accessLogFormats_t* get_dynlog_accessLogFormats(dynlog_t *out) {
    return out->accessLogFormats;
}
int add_item_dynlog_servers_item_listens(dynlog_servers_item_listens_t *src, dynlog_servers_item_listens_item_t item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynlog_servers_item_listens_t* create_dynlog_servers_item_listens(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(dynlog_servers_item_listens_item_t));
}
void set_dynlog_servers_item_listens(dynlog_servers_item_t* obj, dynlog_servers_item_listens_t* field) {
    obj->listens = field;
}
int add_item_dynlog_servers_item_serverNames(dynlog_servers_item_serverNames_t *src, dynlog_servers_item_serverNames_item_t item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynlog_servers_item_serverNames_t* create_dynlog_servers_item_serverNames(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(dynlog_servers_item_serverNames_item_t));
}
void set_dynlog_servers_item_serverNames(dynlog_servers_item_t* obj, dynlog_servers_item_serverNames_t* field) {
    obj->serverNames = field;
}
int add_item_dynlog_servers_item_locations(dynlog_servers_item_locations_t *src, dynlog_servers_item_locations_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynlog_servers_item_locations_t* create_dynlog_servers_item_locations(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(dynlog_servers_item_locations_item_t*));
}
void set_dynlog_servers_item_locations(dynlog_servers_item_t* obj, dynlog_servers_item_locations_t* field) {
    obj->locations = field;
}
dynlog_servers_item_t* create_dynlog_servers_item(njt_pool_t *pool) {
    dynlog_servers_item_t* out = njt_palloc(pool, sizeof(dynlog_servers_item_t));
    memset(out, 0, sizeof(dynlog_servers_item_t));
    return out;
}
int add_item_dynlog_servers(dynlog_servers_t *src, dynlog_servers_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynlog_servers_t* create_dynlog_servers(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(dynlog_servers_item_t*));
}
void set_dynlog_servers(dynlog_t* obj, dynlog_servers_t* field) {
    obj->servers = field;
}
int add_item_dynlog_accessLogFormats(dynlog_accessLogFormats_t *src, dynlog_accessLogFormats_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

dynlog_accessLogFormats_t* create_dynlog_accessLogFormats(njt_pool_t *pool, size_t length) {
    return njt_array_create(pool, length, sizeof(dynlog_accessLogFormats_item_t*));
}
void set_dynlog_accessLogFormats(dynlog_t* obj, dynlog_accessLogFormats_t* field) {
    obj->accessLogFormats = field;
}
dynlog_t* create_dynlog(njt_pool_t *pool) {
    dynlog_t* out = njt_palloc(pool, sizeof(dynlog_t));
    memset(out, 0, sizeof(dynlog_t));
    return out;
}

static void to_oneline_json_dynlog_servers_item_listens_item(dynlog_servers_item_listens_item_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}

static void to_oneline_json_dynlog_servers_item_listens(dynlog_servers_item_listens_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_STR) && (&((dynlog_servers_item_listens_item_t*)out->elts)[i]) == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_dynlog_servers_item_listens_item((&((dynlog_servers_item_listens_item_t*)out->elts)[i]), buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynlog_servers_item_serverNames_item(dynlog_servers_item_serverNames_item_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_sprintf(cur, "\"%V\"", *out);
    buf->len += (*out)->len + 2;
}

static void to_oneline_json_dynlog_servers_item_serverNames(dynlog_servers_item_serverNames_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_STR) && (&((dynlog_servers_item_serverNames_item_t*)out->elts)[i]) == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_dynlog_servers_item_serverNames_item((&((dynlog_servers_item_serverNames_item_t*)out->elts)[i]), buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}
// to_oneline_json_REF

static void to_oneline_json_dynlog_servers_item_locations_item(dynlog_servers_item_locations_item_t *out, njt_str_t* buf, njt_int_t flags) {
    to_oneline_json_locationDef(out, buf, flags);
}

static void to_oneline_json_dynlog_servers_item_locations(dynlog_servers_item_locations_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((dynlog_servers_item_locations_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_dynlog_servers_item_locations_item(((dynlog_servers_item_locations_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynlog_servers_item(dynlog_servers_item_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->listens) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"listens\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynlog_servers_item_listens((out->listens), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->serverNames) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"serverNames\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynlog_servers_item_serverNames((out->serverNames), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->locations) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"locations\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynlog_servers_item_locations((out->locations), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}

static void to_oneline_json_dynlog_servers(dynlog_servers_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((dynlog_servers_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_dynlog_servers_item(((dynlog_servers_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}
// to_oneline_json_REF

static void to_oneline_json_dynlog_accessLogFormats_item(dynlog_accessLogFormats_item_t *out, njt_str_t* buf, njt_int_t flags) {
    to_oneline_json_accessLogFormat(out, buf, flags);
}

static void to_oneline_json_dynlog_accessLogFormats(dynlog_accessLogFormats_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        if ((flags & OMIT_NULL_OBJ) && ((dynlog_accessLogFormats_item_t**)out->elts)[i] == NULL) {
            omit = 1;
        }
        if (omit == 0) {
            to_oneline_json_dynlog_accessLogFormats_item(((dynlog_accessLogFormats_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_dynlog(dynlog_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->servers) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"servers\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynlog_servers((out->servers), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    if ((flags & OMIT_NULL_ARRAY) && (out->accessLogFormats) == NULL) {
        omit = 1;
    }
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"accessLogFormats\":");
        buf->len = cur - buf->data;
        to_oneline_json_dynlog_accessLogFormats((out->accessLogFormats), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
dynlog_t* json_parse_dynlog(njt_pool_t *pool, const njt_str_t *json_string, njt_str_t *err_str) {
    dynlog_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_palloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_str);
        if (parse_result == JSMN_ERROR_INVAL || parse_result == JSMN_ERROR_PART) {
            // njt_pfree(pool, token_buffer);
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            printf("max_token_number: %ld\n", max_token_number);
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_palloc(pool, sizeof(dynlog_t));;
    if (parse_dynlog(pool, parse_state, out, err_str)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_dynlog(njt_pool_t *pool, dynlog_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_palloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_dynlog(out, &str_len, flags);
    json_str->data = njt_palloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_dynlog(out, json_str, flags);
    return json_str;
}
