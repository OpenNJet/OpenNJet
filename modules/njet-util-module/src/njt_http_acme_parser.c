

/* This file was generated by JSON Schema to C.
 * Any changes made to it will be lost on regeneration. 

 * Copyright (C) 2021-2023  TMLake(Beijing) Technology Co., Ltd.
 */

#include "njt_http_acme_parser.h"
#include "njt_core.h"
#include "js2c_njet_builtins.h"
/* ========================== Generated parsers ========================== */


static bool parse_acme_api_action(njt_pool_t *pool, parse_state_t *parse_state, acme_api_action_t *out, js2c_parse_error_t *err_ret) {
    js2c_check_type(JSMN_STRING);
    if (current_string_is(parse_state, "generateCertificate")) {
        *out = ACME_API_ACTION_GENERATE_CERTIFICATE;
    } else if (current_string_is(parse_state, "deleteCertificate")) {
        *out = ACME_API_ACTION_DELETE_CERTIFICATE;
    } else {
        LOG_ERROR_JSON_PARSE(UNKNOWN_ENUM_VALUE_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown enum value in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
        return true;
    }
    parse_state->current_token += 1;
    return false;
}


static bool parse_acme_api_domains_item_server_name(njt_pool_t *pool, parse_state_t *parse_state, acme_api_domains_item_server_name_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
        ((&((acme_api_domains_item_server_name_item_t*)out->elts)[i]))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
        js2c_malloc_check(((&((acme_api_domains_item_server_name_item_t*)out->elts)[i]))->data);
        ((&((acme_api_domains_item_server_name_item_t*)out->elts)[i]))->len = token_size;
        if (builtin_parse_string(pool, parse_state, (&((acme_api_domains_item_server_name_item_t*)out->elts)[i]), 1, ((&((acme_api_domains_item_server_name_item_t*)out->elts)[i]))->len, err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_acme_api_domains_item_server_addr(njt_pool_t *pool, parse_state_t *parse_state, acme_api_domains_item_server_addr_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
        ((&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
        js2c_malloc_check(((&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]))->data);
        ((&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]))->len = token_size;
        if (builtin_parse_string(pool, parse_state, (&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]), 0, ((&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]))->len, err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_acme_api_domains_item(njt_pool_t *pool, parse_state_t *parse_state, acme_api_domains_item_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "server_name")) {
            js2c_check_field_set(out->is_server_name_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "server_name";
            out->server_name = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(njt_str_t));
            js2c_malloc_check(out->server_name);

            if (parse_acme_api_domains_item_server_name(pool, parse_state, (out->server_name), err_ret)) {
                return true;
            }
            out->is_server_name_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "server_addr")) {
            js2c_check_field_set(out->is_server_addr_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "server_addr";
            out->server_addr = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(njt_str_t));
            js2c_malloc_check(out->server_addr);

            if (parse_acme_api_domains_item_server_addr(pool, parse_state, (out->server_addr), err_ret)) {
                return true;
            }
            out->is_server_addr_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "ca_type")) {
            js2c_check_field_set(out->is_ca_type_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "ca_type";
            js2c_null_check();
            int token_size =  CURRENT_STRING_LENGTH(parse_state) ;
            ((&out->ca_type))->data = (u_char*)njt_pcalloc(pool, (size_t)(token_size + 1));
            js2c_malloc_check(((&out->ca_type))->data);
            ((&out->ca_type))->len = token_size;
            if (builtin_parse_string(pool, parse_state, (&out->ca_type), 0, ((&out->ca_type))->len, err_ret)) {
                return true;
            }
            out->is_ca_type_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_server_name_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': server_name", parse_state->current_key);
        return true;
    }
    if (!out->is_server_addr_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': server_addr", parse_state->current_key);
        return true;
    }
    // set default
    if (!out->is_ca_type_set) {
        size_t token_size = strlen("None");
        (out->ca_type).data = (u_char*)njt_pcalloc(pool, token_size + 1);
        js2c_malloc_check((out->ca_type).data);
        (out->ca_type).len = token_size;
        if (out->ca_type.len == 0) {
            (out->ca_type).data[0] = 0;
        }
        if (token_size > 0) {
            njt_memcpy(out->ca_type.data, "None", token_size);
        }
    }
    parse_state->current_token = saved_current_token;
    return false;
}


static bool parse_acme_api_domains(njt_pool_t *pool, parse_state_t *parse_state, acme_api_domains_t *out, js2c_parse_error_t *err_ret) {
    int i;
    js2c_check_type(JSMN_ARRAY);
    const int n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        ((acme_api_domains_item_t**)out->elts)[i] = njt_pcalloc(pool, sizeof(acme_api_domains_item_t));
        memset(((acme_api_domains_item_t**)out->elts)[i], 0, sizeof(acme_api_domains_item_t));
        if (parse_acme_api_domains_item(pool, parse_state, ((acme_api_domains_item_t**)out->elts)[i], err_ret)) {
            return true;
        }
        out->nelts ++;
    }
    return false;
}


static bool parse_acme_api(njt_pool_t *pool, parse_state_t *parse_state, acme_api_t *out, js2c_parse_error_t *err_ret) {
    njt_uint_t i;

    js2c_check_type(JSMN_OBJECT);
    const int object_start_token = parse_state->current_token;
    const uint64_t n = parse_state->tokens[parse_state->current_token].size;
    parse_state->current_token += 1;
    for (i = 0; i < n; ++i) {
        js2c_key_children_check_for_obj();
        if (current_string_is(parse_state, "action")) {
            js2c_check_field_set(out->is_action_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "action";
            if (parse_acme_api_action(pool, parse_state, (&out->action), err_ret)) {
                return true;
            }
            out->is_action_set = 1;
            parse_state->current_key = saved_key;
        } else if (current_string_is(parse_state, "domains")) {
            js2c_check_field_set(out->is_domains_set);
            parse_state->current_token += 1;
            const char* saved_key = parse_state->current_key;
            parse_state->current_key = "domains";
            out->domains = njt_array_create(pool, parse_state->tokens[parse_state->current_token].size ,sizeof(acme_api_domains_item_t*));
            js2c_malloc_check(out->domains);

            if (parse_acme_api_domains(pool, parse_state, (out->domains), err_ret)) {
                return true;
            }
            out->is_domains_set = 1;
            parse_state->current_key = saved_key;
        } else {
            LOG_ERROR_JSON_PARSE(UNKNOWN_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Unknown field in '%s': %.*s", parse_state->current_key, CURRENT_STRING_FOR_ERROR(parse_state));
            return true;
        }
    }
    const int saved_current_token = parse_state->current_token;
    parse_state->current_token = object_start_token;
    if (!out->is_action_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': action", parse_state->current_key);
        return true;
    }
    if (!out->is_domains_set) {
        LOG_ERROR_JSON_PARSE(MISSING_REQUIRED_FIELD_ERR, parse_state->current_key, CURRENT_TOKEN(parse_state).start, "Missing required field in '%s': domains", parse_state->current_key);
        return true;
    }
    parse_state->current_token = saved_current_token;
    return false;
}

// BEGIN GET_JSON_LENGTH ENUM

static void get_json_length_acme_api_action(njt_pool_t *pool, acme_api_action_t *out, size_t *length, njt_int_t flags) {
    if (*out == ACME_API_ACTION_GENERATE_CERTIFICATE) {
        // "generateCertificate"
        *length += 19 + 2;
        return;
    }
    if (*out == ACME_API_ACTION_DELETE_CERTIFICATE) {
        // "deleteCertificate"
        *length += 17 + 2;
        return;
    }
}

static void get_json_length_acme_api_domains_item_server_name_item(njt_pool_t *pool, acme_api_domains_item_server_name_item_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_acme_api_domains_item_server_name(njt_pool_t *pool, acme_api_domains_item_server_name_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((acme_api_domains_item_server_name_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_acme_api_domains_item_server_name_item(pool, (&((acme_api_domains_item_server_name_item_t*)out->elts)[i]), length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_acme_api_domains_item_server_addr_item(njt_pool_t *pool, acme_api_domains_item_server_addr_item_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_acme_api_domains_item_server_addr(njt_pool_t *pool, acme_api_domains_item_server_addr_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((acme_api_domains_item_server_addr_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_acme_api_domains_item_server_addr_item(pool, (&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]), length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_acme_api_domains_item_ca_type(njt_pool_t *pool, acme_api_domains_item_ca_type_t *out, size_t *length, njt_int_t flags) {
    njt_str_t *dst = handle_escape_on_write(pool, out);
    *length += dst->len + 2; //  "str" 
}

static void get_json_length_acme_api_domains_item(njt_pool_t *pool, acme_api_domains_item_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_server_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->server_name) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (11 + 3); // "server_name": 
        get_json_length_acme_api_domains_item_server_name(pool, (out->server_name), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_server_addr_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->server_addr) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (11 + 3); // "server_addr": 
        get_json_length_acme_api_domains_item_server_addr(pool, (out->server_addr), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_ca_type_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->ca_type.data) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (7 + 3); // "ca_type": 
        get_json_length_acme_api_domains_item_ca_type(pool, (&out->ca_type), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}

static void get_json_length_acme_api_domains(njt_pool_t *pool, acme_api_domains_t *out, size_t *length, njt_int_t flags) {
    njt_uint_t i;
    njt_uint_t omit;
    njt_int_t count = 0;
    if (out == NULL) {
        *length += 2; // "[]"
        return;
    }
    *length += 2; // "[]"
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((acme_api_domains_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            get_json_length_acme_api_domains_item(pool, ((acme_api_domains_item_t**)out->elts)[i], length, flags);
            *length += 1; // ","
            count++; // ","
        }
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
}

static void get_json_length_acme_api(njt_pool_t *pool, acme_api_t *out, size_t *length, njt_int_t flags) {
    if (out == NULL) {
        *length += 4; // null
        return;
    }
    *length += 1;
    njt_int_t omit;
    njt_int_t count = 0;
    omit = 0;
    omit = out->is_action_set ? 0 : 1;
    if (omit == 0) {
        *length += (6 + 3); // "action": 
        get_json_length_acme_api_action(pool, (&out->action), length, flags);
        *length += 1; // ","
        count++;
    }
    omit = 0;
    omit = out->is_domains_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->domains) == NULL ? 1 : omit;
    if (omit == 0) {
        *length += (7 + 3); // "domains": 
        get_json_length_acme_api_domains(pool, (out->domains), length, flags);
        *length += 1; // ","
        count++;
    }
    if (count != 0) {
        *length -= 1; // "\b"
    }
    *length += 1;
}
acme_api_domains_item_server_name_item_t* get_acme_api_domains_item_server_name_item(acme_api_domains_item_server_name_t *out, size_t idx) {
    return &((acme_api_domains_item_server_name_item_t*)out->elts)[idx];

}
acme_api_domains_item_server_addr_item_t* get_acme_api_domains_item_server_addr_item(acme_api_domains_item_server_addr_t *out, size_t idx) {
    return &((acme_api_domains_item_server_addr_item_t*)out->elts)[idx];

}

acme_api_domains_item_server_name_t* get_acme_api_domains_item_server_name(acme_api_domains_item_t *out) {
    return out->server_name;
}

acme_api_domains_item_server_addr_t* get_acme_api_domains_item_server_addr(acme_api_domains_item_t *out) {
    return out->server_addr;
}

acme_api_domains_item_ca_type_t* get_acme_api_domains_item_ca_type(acme_api_domains_item_t *out) {
    return &out->ca_type;
}
acme_api_domains_item_t* get_acme_api_domains_item(acme_api_domains_t *out, size_t idx) {
    return ((acme_api_domains_item_t**)out->elts)[idx];

}

acme_api_action_t get_acme_api_action(acme_api_t *out) {
    return out->action;
}

acme_api_domains_t* get_acme_api_domains(acme_api_t *out) {
    return out->domains;
}
void set_acme_api_action(acme_api_t* obj, acme_api_action_t field) {
    obj->action = field;
    obj->is_action_set = 1;
}
int add_item_acme_api_domains_item_server_name(acme_api_domains_item_server_name_t *src, acme_api_domains_item_server_name_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, item, src->size);
    return NJT_OK;
}

acme_api_domains_item_server_name_t* create_acme_api_domains_item_server_name(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(acme_api_domains_item_server_name_item_t));
}
void set_acme_api_domains_item_server_name(acme_api_domains_item_t* obj, acme_api_domains_item_server_name_t* field) {
    obj->server_name = field;
    obj->is_server_name_set = 1;
}
int add_item_acme_api_domains_item_server_addr(acme_api_domains_item_server_addr_t *src, acme_api_domains_item_server_addr_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, item, src->size);
    return NJT_OK;
}

acme_api_domains_item_server_addr_t* create_acme_api_domains_item_server_addr(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(acme_api_domains_item_server_addr_item_t));
}
void set_acme_api_domains_item_server_addr(acme_api_domains_item_t* obj, acme_api_domains_item_server_addr_t* field) {
    obj->server_addr = field;
    obj->is_server_addr_set = 1;
}
void set_acme_api_domains_item_ca_type(acme_api_domains_item_t* obj, acme_api_domains_item_ca_type_t* field) {
    njt_memcpy(&obj->ca_type, field, sizeof(njt_str_t));
    obj->is_ca_type_set = 1;
}
acme_api_domains_item_t* create_acme_api_domains_item(njt_pool_t *pool) {
    acme_api_domains_item_t* out = njt_pcalloc(pool, sizeof(acme_api_domains_item_t));
    return out;
}
int add_item_acme_api_domains(acme_api_domains_t *src, acme_api_domains_item_t* item) {
    void *new = njt_array_push(src);
    if (new == NULL) {
        return NJT_ERROR;
    }
    njt_memcpy(new, &item, src->size);
    return NJT_OK;
}

acme_api_domains_t* create_acme_api_domains(njt_pool_t *pool, size_t nelts) {
    return njt_array_create(pool, nelts, sizeof(acme_api_domains_item_t*));
}
void set_acme_api_domains(acme_api_t* obj, acme_api_domains_t* field) {
    obj->domains = field;
    obj->is_domains_set = 1;
}
acme_api_t* create_acme_api(njt_pool_t *pool) {
    acme_api_t* out = njt_pcalloc(pool, sizeof(acme_api_t));
    return out;
}

static void to_oneline_json_acme_api_action(njt_pool_t *pool, acme_api_action_t *out, njt_str_t* buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    if (*out == ACME_API_ACTION_GENERATE_CERTIFICATE) {
        cur = njt_sprintf(cur, "\"generateCertificate\"");
        buf->len += 19 + 2;
        return;
    }
    if (*out == ACME_API_ACTION_DELETE_CERTIFICATE) {
        cur = njt_sprintf(cur, "\"deleteCertificate\"");
        buf->len += 17 + 2;
        return;
    }
}

static void to_oneline_json_acme_api_domains_item_server_name_item(njt_pool_t *pool, acme_api_domains_item_server_name_item_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_acme_api_domains_item_server_name(njt_pool_t *pool, acme_api_domains_item_server_name_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((acme_api_domains_item_server_name_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_acme_api_domains_item_server_name_item(pool, (&((acme_api_domains_item_server_name_item_t*)out->elts)[i]), buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_acme_api_domains_item_server_addr_item(njt_pool_t *pool, acme_api_domains_item_server_addr_item_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_acme_api_domains_item_server_addr(njt_pool_t *pool, acme_api_domains_item_server_addr_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_STR) && (&((acme_api_domains_item_server_addr_item_t*)out->elts)[i])->data == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_acme_api_domains_item_server_addr_item(pool, (&((acme_api_domains_item_server_addr_item_t*)out->elts)[i]), buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_acme_api_domains_item_ca_type(njt_pool_t *pool, acme_api_domains_item_ca_type_t *out, njt_str_t *buf, njt_int_t flags) {
    u_char* cur = buf->data + buf->len;
    njt_str_t *dst = handle_escape_on_write(pool, out);
    cur = njt_sprintf(cur, "\"%V\"", dst);
    buf->len = cur - buf->data;
}

static void to_oneline_json_acme_api_domains_item(njt_pool_t *pool, acme_api_domains_item_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_server_name_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->server_name) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"server_name\":");
        buf->len = cur - buf->data;
        to_oneline_json_acme_api_domains_item_server_name(pool, (out->server_name), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_server_addr_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->server_addr) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"server_addr\":");
        buf->len = cur - buf->data;
        to_oneline_json_acme_api_domains_item_server_addr(pool, (out->server_addr), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_ca_type_set ? 0 : 1;
    omit = (flags & OMIT_NULL_STR) && (out->ca_type.data) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"ca_type\":");
        buf->len = cur - buf->data;
        to_oneline_json_acme_api_domains_item_ca_type(pool, (&out->ca_type), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}

static void to_oneline_json_acme_api_domains(njt_pool_t *pool, acme_api_domains_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char *cur = buf->data + buf->len;
    njt_uint_t i;
    if (out == NULL || out->nelts == 0) {
        cur = njt_sprintf(cur, "[]");
        buf->len += 2;
        return;
    }
    cur = njt_sprintf(cur,  "[");
    buf->len ++;
    for (i = 0; i < out->nelts; ++i) {
        omit = 0;
        omit = ((flags & OMIT_NULL_OBJ) && ((acme_api_domains_item_t**)out->elts)[i] == NULL) ? 1 : 0;
        if (omit == 0) {
            to_oneline_json_acme_api_domains_item(pool, ((acme_api_domains_item_t**)out->elts)[i], buf, flags);
            cur = buf->data + buf->len;
            cur = njt_sprintf(cur, ",");
            buf->len ++;
        }
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur,  "]");
    buf->len ++;
}

static void to_oneline_json_acme_api(njt_pool_t *pool, acme_api_t *out, njt_str_t* buf, njt_int_t flags) {
    njt_int_t omit;
    u_char* cur = buf->data + buf->len;
    if (out == NULL) {
        cur = njt_sprintf(cur, "null");
        buf->len += 4;
        return;
    }
    cur = njt_sprintf(cur, "{");
    buf->len ++;
    omit = 0;
    omit = out->is_action_set ? 0 : 1;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"action\":");
        buf->len = cur - buf->data;
        to_oneline_json_acme_api_action(pool, (&out->action), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    omit = 0;
    omit = out->is_domains_set ? 0 : 1;
    omit = (flags & OMIT_NULL_ARRAY) && (out->domains) == NULL ? 1 : omit;
    if (omit == 0) {
        cur = njt_sprintf(cur, "\"domains\":");
        buf->len = cur - buf->data;
        to_oneline_json_acme_api_domains(pool, (out->domains), buf, flags);
        cur = buf->data + buf->len;
        cur = njt_sprintf(cur, ",");
        buf->len ++;
    }
    cur--;
    if (cur[0] == ',') {
        buf->len --;
    } else {
        cur ++;
    }
    cur = njt_sprintf(cur, "}");
    buf->len ++;
}
acme_api_t* json_parse_acme_api(njt_pool_t *pool, const njt_str_t *json_string, js2c_parse_error_t *err_ret) {
    acme_api_t* out;
    parse_state_t parse_state_var;
    parse_state_t *parse_state = &parse_state_var;
    uint64_t max_token_number = 1024;
    jsmntok_t *token_buffer;
    int parse_result;
    for ( ; /* parse unsuccessful */; ) {
        token_buffer = njt_pcalloc(pool, sizeof(jsmntok_t)*max_token_number);
        parse_result = builtin_parse_json_string(pool, parse_state, token_buffer, max_token_number, (char *)json_string->data, json_string->len, err_ret);
        if (parse_result == JSMN_ERROR_INVAL) {
            LOG_ERROR_JSON_PARSE(INVALID_JSON_CHAR_ERR, "", -1, "%s", "Invalid character inside JSON string");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_PART) {
            LOG_ERROR_JSON_PARSE(PARTIAL_JSON_ERR, "", -1, "%s", "The string is not a full JSON packet, more bytes expected");
            return NULL;
        }
        if (parse_result == JSMN_ERROR_NOMEM) {
            max_token_number += max_token_number;
            continue;
        }
        if (parse_result == 0) {
            LOG_ERROR_JSON_PARSE(NULL_JSON_ERR, "", 0, "String did not contain %s JSON tokens", "any");
            return NULL;
        }
        break; // parse success
    }
    out = njt_pcalloc(pool, sizeof(acme_api_t));;
    if (parse_acme_api(pool, parse_state, out, err_ret)) {
        return NULL;
    }
    return out;
}

njt_str_t* to_json_acme_api(njt_pool_t *pool, acme_api_t* out, njt_int_t flags) {
    njt_str_t *json_str;
    json_str = njt_pcalloc(pool, sizeof(njt_str_t));
    size_t str_len = 0;
    get_json_length_acme_api(pool, out, &str_len, flags);
    json_str->data = (u_char*)njt_pcalloc(pool, str_len + 1);
    json_str->len = 0;
    to_oneline_json_acme_api(pool, out, json_str, flags);
    return json_str;
}