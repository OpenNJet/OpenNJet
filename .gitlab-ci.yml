stages:
  - build_bin

build_bin:
  stage: build_bin
  image: gcc:4.8.5
  script:
    - chmod +x ./build_cc.sh ./configure
    - ./build_cc.sh conf $CI_COMMIT_SHA
    - make -j `nproc`
  artifacts:
    paths:
      - objs/njet
    expire_in: 1 week